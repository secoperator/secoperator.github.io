<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://secoperator.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://secoperator.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-05-18T10:14:08+00:00</updated><id>https://secoperator.github.io/feed.xml</id><title type="html">secoperator</title><subtitle>Notes on vulnerability research, browser internals and modern exploitation. </subtitle><entry><title type="html">LPE a root en Android: introducción y cronología de vulnerabilidades</title><link href="https://secoperator.github.io/blog/2026/android-lpe-root/" rel="alternate" type="text/html" title="LPE a root en Android: introducción y cronología de vulnerabilidades"/><published>2026-05-18T10:00:00+00:00</published><updated>2026-05-18T10:00:00+00:00</updated><id>https://secoperator.github.io/blog/2026/android-lpe-root</id><content type="html" xml:base="https://secoperator.github.io/blog/2026/android-lpe-root/"><![CDATA[<h2 id="introducción">Introducción</h2> <p>En seguridad ofensiva, <strong>Local Privilege Escalation (LPE)</strong> describe el paso de un contexto de baja confianza (una app sin permisos sensibles, un proceso del sandbox, una shell ADB sin privilegios) a otro contexto privilegiado en el mismo dispositivo. En Android, el objetivo final suele ser uno de estos tres:</p> <ul> <li><strong>UID <code class="language-plaintext highlighter-rouge">root</code></strong> (UID 0): control total sobre el espacio de usuario.</li> <li><strong>Contexto de kernel</strong> (ring 0 efectivo): se rompen además las restricciones de SELinux, capabilities y seccomp.</li> <li><strong>Bypass de SELinux</strong> desde un dominio confinado a un dominio sin confinar (por ejemplo <code class="language-plaintext highlighter-rouge">untrusted_app</code> → <code class="language-plaintext highlighter-rouge">init</code> o <code class="language-plaintext highlighter-rouge">vendor_init</code>).</li> </ul> <p>En la práctica, un “root exploit” en Android moderno casi siempre implica encadenar varias primitivas, no sólo escalar UID:</p> <ol> <li><strong>Salir del sandbox de la app</strong> (escapar de <code class="language-plaintext highlighter-rouge">untrusted_app</code> / Zygote / <code class="language-plaintext highlighter-rouge">isolated_app</code>).</li> <li><strong>Comprometer el kernel</strong> o un servicio privilegiado (Binder, <code class="language-plaintext highlighter-rouge">system_server</code>, drivers de GPU/DSP).</li> <li><strong>Eludir SELinux</strong> y mitigaciones como CFI, KASLR, hypervisor (<code class="language-plaintext highlighter-rouge">pKVM</code>), MTE en ARMv9, <em>Page Table Isolation</em>, y <em>Kernel Lockdown</em>.</li> <li><strong>Persistir</strong>, lo cual con Verified Boot / dm-verity es cada vez más difícil sin desbloquear el bootloader.</li> </ol> <h3 id="por-qué-android-es-un-caso-distinto-a-linux-a-secas">Por qué Android es un caso distinto a “Linux a secas”</h3> <p>Android comparte kernel con Linux, así que muchos bugs de kernel upstream (netfilter, eBPF, io_uring, ALSA, etc.) afectan también a teléfonos. Pero el resto del stack es muy específico:</p> <ul> <li><strong>Binder</strong> es el IPC central; cualquier UAF o race aquí es una puerta directa a <code class="language-plaintext highlighter-rouge">system_server</code>.</li> <li>Los drivers de <strong>GPU vendor</strong> (Mali de ARM, Adreno de Qualcomm, PowerVR) y los de <strong>DSP</strong> (Hexagon, Cypress) están expuestos al sandbox de apps y son la superficie más explotada por <em>spyware</em> comercial.</li> <li><strong>SELinux en modo enforcing</strong> está activo desde Android 5; un bug de kernel sin bypass SELinux a menudo no es suficiente para <code class="language-plaintext highlighter-rouge">root</code> “real”.</li> <li><strong>GKI (Generic Kernel Image)</strong> desde Android 11 ha homogeneizado el kernel base, pero los <strong>modules vendor</strong> siguen siendo el eslabón débil.</li> <li><strong>Play Protect</strong>, <strong>Knox</strong>, <strong>TrustZone/TEE</strong> y el coprocesador <strong>Titan M/M2</strong> añaden capas de defensa fuera del kernel.</li> </ul> <p>Por todo esto, en Android distinguimos cuatro tipos de vulnerabilidad que llevan a root:</p> <table> <thead> <tr> <th>Tipo</th> <th>Superficie</th> <th>Ejemplo típico</th> </tr> </thead> <tbody> <tr> <td>Userland → System</td> <td>Binder, <code class="language-plaintext highlighter-rouge">system_server</code>, <code class="language-plaintext highlighter-rouge">installd</code>, <code class="language-plaintext highlighter-rouge">run-as</code></td> <td>CVE-2024-0044</td> </tr> <tr> <td>Userland → Kernel (kernel core)</td> <td>syscalls genéricas Linux (netfilter, eBPF, io_uring)</td> <td>CVE-2021-22555</td> </tr> <tr> <td>Userland → Kernel (vendor)</td> <td>Mali, Adreno, DSP, MTK, Samsung NPU</td> <td>CVE-2023-26083</td> </tr> <tr> <td>Bootchain / firmware</td> <td>Bootloader, fastboot, TEE</td> <td>CVE-2024-32896</td> </tr> </tbody> </table> <p>A continuación una cronología centrada en los últimos años, con énfasis en bugs <strong>explotados en el mundo real</strong> o con PoC público que se sabe que <strong>funciona en Android</strong>. Si un CVE es de kernel Linux pero no llegó a impactar dispositivos Android reales (por ejemplo porque sólo afecta a Ubuntu con <code class="language-plaintext highlighter-rouge">overlayfs</code> modificado), no aparece.</p> <hr/> <h2 id="recordatorios-históricos-pre-2020">Recordatorios históricos (pre-2020)</h2> <p>Sólo como referencia, porque marcaron la cultura del “rooting”:</p> <ul> <li><strong>CVE-2014-3153</strong> — <em>Towelroot</em> (geohot). Bug en <code class="language-plaintext highlighter-rouge">futex</code> del kernel Linux; rooteó casi cualquier Android &lt; 4.4.4.</li> <li><strong>CVE-2015-3636</strong> — <em>PingPongRoot</em>. UAF en sockets <code class="language-plaintext highlighter-rouge">ping</code> del kernel, Android 5.x.</li> <li><strong>CVE-2016-5195</strong> — <em>Dirty COW</em>. COW race en el kernel Linux; portada a Android como <strong>DirtyCow</strong> y luego <strong>DirtyCopy</strong>.</li> <li><strong>CVE-2019-2215</strong> — <em>Bad Binder</em> (Project Zero, Maddie Stone). UAF en el driver Binder, explotado in-the-wild por NSO contra Pixel 1/2, Galaxy S7/S8/S9, Huawei P20 y más.</li> </ul> <hr/> <h2 id="2020">2020</h2> <ul> <li><strong>CVE-2020-0041</strong> <em>(Binder)</em> — Out-of-bounds write en <code class="language-plaintext highlighter-rouge">binder_release_work</code> permitiendo escapar del sandbox de app a <code class="language-plaintext highlighter-rouge">system_server</code>. Publicado por GRIMM con PoC encadenando una segunda vuln para kernel.</li> <li><strong>CVE-2020-0069</strong> — <strong>MediaTek-SU</strong>. Falla en el driver <code class="language-plaintext highlighter-rouge">CMDQ</code> de MediaTek (<code class="language-plaintext highlighter-rouge">/dev/mtk_cmdq</code>) que daba <strong>root + bypass SELinux</strong> trivialmente desde una app sin permisos. Afectó a decenas de modelos con SoCs MT6735, MT6737, MT6739, MT6753, MT6757, MT6761, MT6762, MT6763, MT6765, MT6771, MT6797 y muchos más. Fue masivo: se vendía como “one-click root” durante años antes de ser parcheado en marzo de 2020.</li> <li><strong>CVE-2020-0423</strong> <em>(Binder, UAF)</em> — Use-after-free en el reaping de procesos, parte del <em>bug class</em> Binder que llevaría a CVE-2021-0920.</li> </ul> <hr/> <h2 id="2021">2021</h2> <p>Año récord para LPE in-the-wild en Android, en parte por la actividad de vendors comerciales (NSO, Cytrox, Intellexa).</p> <ul> <li><strong>CVE-2021-1048</strong> <em>(Linux/Android, epoll UAF)</em> — UAF en <code class="language-plaintext highlighter-rouge">epoll</code> por race en <code class="language-plaintext highlighter-rouge">ep_loop_check_proc</code>. Explotado <strong>in-the-wild</strong> según Project Zero (TAG-0; “Forced Entry”-era).</li> <li><strong>CVE-2021-0920</strong> <em>(Binder)</em> — UAF en el ciclo de <code class="language-plaintext highlighter-rouge">fd</code> de Binder. Documentado como <strong>explotado en ataques dirigidos</strong> contra Android antes del parche.</li> <li><strong>CVE-2021-22600</strong> — Double-free en <code class="language-plaintext highlighter-rouge">AF_PACKET</code> (<code class="language-plaintext highlighter-rouge">packet_set_ring</code>). Reportado por @vv474172261, recompensado por Google con <strong>$157,500</strong> (récord en su momento para una LPE Android). PoC público disponible.</li> <li><strong>CVE-2021-3490</strong> — eBPF: verificación insuficiente de operaciones ALU 32-bit, permite OOB R/W. Funcional en kernels Android con eBPF habilitado para apps no-root (caso de algunos OEM).</li> <li><strong>CVE-2021-22555</strong> — <em>“Five Years of Bug-doors”</em>: heap OOB write en <code class="language-plaintext highlighter-rouge">netfilter</code> (<code class="language-plaintext highlighter-rouge">x_tables</code>). Linux y Android. Una de las primitivas más limpias de la década.</li> <li><strong>CVE-2021-39793</strong> — UAF en el GPU Mali (<code class="language-plaintext highlighter-rouge">kbase_jd_user_buf_pin_pages</code>), reportado por Man Yue Mo (GitHub Security Lab).</li> </ul> <hr/> <h2 id="2022">2022</h2> <ul> <li><strong>CVE-2022-20186</strong> — OOB write en el driver Mali (<code class="language-plaintext highlighter-rouge">kbase_mem_alias</code>). Cadena completa publicada por Man Yue Mo contra un Pixel 6 stock.</li> <li><strong>CVE-2022-38181</strong> — Mali GPU UAF. <strong>Explotada in-the-wild</strong> según el TAG report de marzo de 2023 (cadena de exploits de un vendor comercial contra Samsung).</li> <li><strong>CVE-2022-22057</strong> — UAF en el driver de GPU <strong>Adreno</strong> (<code class="language-plaintext highlighter-rouge">kgsl</code>), reportado por Man Yue Mo. Afectó a SoCs Qualcomm en Android.</li> <li><strong>CVE-2022-25636</strong> — Heap OOB write en <code class="language-plaintext highlighter-rouge">nf_tables</code> (<code class="language-plaintext highlighter-rouge">nft_fwd_dup_netdev_offload</code>). Aplicable a varios kernels Android.</li> <li><strong>CVE-2022-32250</strong> — UAF en <code class="language-plaintext highlighter-rouge">nft_tables</code> (Linux/Android). Cadena pública contra Ubuntu, adaptable a Android.</li> <li><strong>CVE-2022-2588</strong> — Double-free en <code class="language-plaintext highlighter-rouge">cls_route</code> del kernel Linux. Funcional contra kernels Android antiguos con QoS habilitado.</li> <li><strong>CVE-2022-0492</strong> — Container escape vía cgroups v1 <code class="language-plaintext highlighter-rouge">release_agent</code>. Relevante para Android principalmente en el contexto de <code class="language-plaintext highlighter-rouge">app_zygote</code> y procesos aislados con cgroups.</li> <li><strong>CVE-2022-22265</strong> — Bug en el driver del <strong>NPU</strong> de Samsung (Exynos). Restringido a dispositivos Samsung con Exynos, escalada de UID <code class="language-plaintext highlighter-rouge">system_server</code>-equivalente a kernel.</li> </ul> <hr/> <h2 id="2023">2023</h2> <ul> <li><strong>CVE-2023-0266</strong> — UAF en ALSA (<code class="language-plaintext highlighter-rouge">snd_ctl_elem_*</code>). <strong>Explotada in-the-wild</strong> según TAG en cadenas contra Samsung Android (junto a CVE-2022-4262 en V8 para la entrada).</li> <li><strong>CVE-2023-26083</strong> — Memory leak en el driver Mali GPU, usada para <strong>infoleak</strong> de direcciones de kernel. Empleada por <strong>Cytrox/Intellexa</strong> para entregar el spyware <strong>Predator</strong> sobre Samsung Galaxy S22, según el reporte de TAG/Amnesty.</li> <li><strong>CVE-2023-21400</strong> — Múltiples bugs en <code class="language-plaintext highlighter-rouge">io_uring</code> (<code class="language-plaintext highlighter-rouge">io_*_rsrc</code>) en kernels Android.</li> <li><strong>CVE-2023-33063</strong> — Use-after-free en el firmware del <strong>DSP Hexagon</strong> (Qualcomm). Marcada por Qualcomm como “may be under limited, targeted exploitation”. Ataques tipo <em>Operation Triangulation</em> pero en Android.</li> <li><strong>CVE-2023-33107</strong> — Integer overflow en el driver <strong>Adreno</strong> (<code class="language-plaintext highlighter-rouge">KGSL_IOCTL_GPUOBJ_IMPORT</code>). <strong>Explotada in-the-wild</strong> según Qualcomm/Google (octubre 2023).</li> <li><strong>CVE-2023-22071</strong> — Buffer overflow en el driver Adreno, también Qualcomm.</li> <li><strong>CVE-2023-35690</strong> — Memory corruption en <code class="language-plaintext highlighter-rouge">Modem</code> (Qualcomm), parcheado en el boletín de seguridad Android de diciembre 2023.</li> <li><strong>CVE-2023-4147</strong> — UAF en <code class="language-plaintext highlighter-rouge">nft_chain</code> de netfilter (Linux/Android).</li> <li><strong>CVE-2023-21256</strong> — LPE en el stack Bluetooth de Android System.</li> </ul> <hr/> <h2 id="2024">2024</h2> <ul> <li><strong>CVE-2024-0044</strong> — <em>“AnyRoot”</em>: error de validación en <code class="language-plaintext highlighter-rouge">run-as</code> (Android 12 y 13) que permite a una shell ADB sin privilegios suplantar a cualquier paquete y leer/escribir sus datos privados. No es UID 0, pero sí <em>de-facto</em> compromiso completo de cualquier app. PoC trivial y muy extendido. Reportado por Meta Red Team X.</li> <li><strong>CVE-2024-31317</strong> — <em>Zygote command injection</em>: inyección en argumentos del <code class="language-plaintext highlighter-rouge">Zygote</code> desde <code class="language-plaintext highlighter-rouge">WindowManager</code>, permite ejecutar código como cualquier UID al que el Zygote pueda forkear, incluyendo <code class="language-plaintext highlighter-rouge">system</code> (UID 1000). Descubierto por Meta Red Team X.</li> <li><strong>CVE-2024-32896</strong> — Bug en el firmware/ROM de Pixel (“Pixel Lock Screen Bypass”), <strong>explotada por herramientas forenses</strong> (Cellebrite). Aunque originalmente reportado contra Pixel internos, fue extendido al resto de Pixel.</li> <li><strong>CVE-2024-29748</strong> — Bug que permite cancelar el factory reset en Pixel, también utilizado en cadenas forenses.</li> <li><strong>CVE-2024-43093</strong> — LPE en el Android Framework (path traversal en <code class="language-plaintext highlighter-rouge">Environment</code>), permitía acceder a directorios con paths con caracteres unicode normalizados. Marcada por Google como “may be under limited, targeted exploitation”.</li> <li><strong>CVE-2024-1086</strong> — UAF en <code class="language-plaintext highlighter-rouge">nft_verdict_init</code> (netfilter, <code class="language-plaintext highlighter-rouge">nf_tables</code>). PoC público de @Notselwyn, kernel Linux 5.14 – 6.6. <strong>Aplicable a kernels Android</strong> que tengan <code class="language-plaintext highlighter-rouge">nf_tables</code> habilitado para <code class="language-plaintext highlighter-rouge">user_namespaces</code> (depende del OEM; AOSP estándar lo restringe pero algunos vendors no).</li> <li><strong>CVE-2024-36971</strong> — UAF en <code class="language-plaintext highlighter-rouge">__dst_negative_advice</code> (Linux netfilter), parcheado en julio 2024 e incluido en el Android Security Bulletin de septiembre 2024 como exploit <em>in-the-wild</em>.</li> <li><strong>CVE-2024-36978</strong> — Bug en <code class="language-plaintext highlighter-rouge">net/sched</code> confirmado afectando kernels Android via patchlevel.</li> </ul> <hr/> <h2 id="2025">2025</h2> <ul> <li><strong>CVE-2025-27363</strong> — OOB write en <strong>FreeType</strong> ≤ 2.13.0 al parsear fuentes TrueType GX. Android distribuye FreeType vulnerable hasta Android 14 patch level marzo 2025. <strong>Explotada in-the-wild</strong> según el boletín de seguridad Android de mayo 2025 (Google fija el flag <em>limited, targeted exploitation</em>). Funciona como RCE → LPE en cadenas modernas.</li> <li><strong>CVE-2025-0072</strong> — Memory corruption en Mali GPU r49p0 – r52p0 (ARM driver), reportada por Project Zero. Pixel y muchos Samsung.</li> <li><strong>CVE-2025-21043</strong> — Buffer overflow en <code class="language-plaintext highlighter-rouge">libimagecodec.quram</code> (Samsung Galaxy). <strong>Explotada in-the-wild</strong> contra Samsung según el reporte de Samsung de septiembre 2025; cadena junto con un bug de mensajería para alcanzar <code class="language-plaintext highlighter-rouge">system_server</code>-equivalente.</li> <li><strong>CVE-2025-21479</strong> y <strong>CVE-2025-21480</strong> — Memory corruption en el GPU <strong>Adreno</strong> (Qualcomm GPU microcode). <strong>Explotadas in-the-wild</strong> según Qualcomm en junio 2025 (vendor comercial no nombrado, dispositivos Snapdragon 8 Gen 1/2/3).</li> <li><strong>CVE-2025-27038</strong> — UAF en Adreno (<code class="language-plaintext highlighter-rouge">process_private</code>), parcheado por Qualcomm en mayo 2025.</li> <li><strong>CVE-2025-48543</strong> — LPE en Android Runtime (ART). Permite a una app no privilegiada escalar a otra app o a UID <code class="language-plaintext highlighter-rouge">system</code> mediante una race durante carga de <code class="language-plaintext highlighter-rouge">dex</code>. Boletín Android octubre 2025.</li> <li><strong>CVE-2025-48530</strong> — Bug en Android System (Bluetooth pairing), encadenable con CVE-2025-48543 para escalada completa.</li> </ul> <hr/> <h2 id="observaciones-generales-de-la-cronología">Observaciones generales de la cronología</h2> <ol> <li><strong>El GPU es la nueva superficie estrella</strong>. Desde 2022, casi todas las cadenas explotadas en el mundo real contra Android usan un bug de <strong>Mali (ARM)</strong> o <strong>Adreno (Qualcomm)</strong>, porque están directamente expuestos a apps sin permisos y los vendors tardan en publicar parches.</li> <li><strong>Binder se sigue rompiendo</strong>, pero menos: GKI, KCFI y los mitigaciones de Project Zero han reducido el ratio.</li> <li><strong>Los bugs de kernel Linux genéricos</strong> (netfilter, eBPF, io_uring, ALSA) <strong>sí impactan Android</strong>, pero su explotación pública suele requerir <code class="language-plaintext highlighter-rouge">user_namespaces</code>, que AOSP por defecto restringe; los vendors que los habilitan amplían la superficie.</li> <li><strong>El bypass de SELinux</strong> rara vez aparece como un CVE propio; suele ser una propiedad emergente de cómo se monta el exploit de kernel (sobreescribir <code class="language-plaintext highlighter-rouge">selinux_state.enforcing</code>, o falsear el contexto del thread actual).</li> <li>La etiqueta <strong>“under limited, targeted exploitation”</strong> en los Android Security Bulletins desde 2023 es un buen indicador de qué CVEs realmente se usaron por vendors comerciales (NSO, Cytrox, Intellexa, Variston).</li> </ol> <hr/> <h2 id="referencias-y-lecturas-recomendadas">Referencias y lecturas recomendadas</h2> <ul> <li>Boletines de seguridad de Android: <a href="https://source.android.com/docs/security/bulletin">https://source.android.com/docs/security/bulletin</a></li> <li>Boletines de Qualcomm: <a href="https://docs.qualcomm.com/product/publicresources/securitybulletin/">https://docs.qualcomm.com/product/publicresources/securitybulletin/</a></li> <li>Boletines de Samsung Mobile: <a href="https://security.samsungmobile.com/securityUpdate.smsb">https://security.samsungmobile.com/securityUpdate.smsb</a></li> <li>Project Zero — <em>Issue Tracker</em> y posts de Man Yue Mo y Maddie Stone sobre Mali, Adreno y Binder.</li> <li>Google TAG: reportes anuales de exploits 0-day in-the-wild.</li> </ul> <blockquote> <p>Nota: esta cronología cubre desde 2020 hasta mayo de 2026. Los CVEs marcados como <em>in-the-wild</em> son los que tanto Google como los vendors han confirmado que se usaron en ataques reales antes del parche; el resto tienen PoC público o privado conocido contra Android.</p> </blockquote>]]></content><author><name></name></author><category term="security"/><category term="android"/><category term="security"/><category term="lpe"/><category term="kernel"/><category term="exploits"/><summary type="html"><![CDATA[Panorama y cronología de Local Privilege Escalation hasta root en Android, incluyendo bugs del kernel Linux que también impactan a dispositivos Android.]]></summary></entry><entry><title type="html">Anatomía de la creación de procesos en Android: del kernel a Zygote y app_process</title><link href="https://secoperator.github.io/blog/2026/android-process-creation/" rel="alternate" type="text/html" title="Anatomía de la creación de procesos en Android: del kernel a Zygote y app_process"/><published>2026-05-18T10:00:00+00:00</published><updated>2026-05-18T10:00:00+00:00</updated><id>https://secoperator.github.io/blog/2026/android-process-creation</id><content type="html" xml:base="https://secoperator.github.io/blog/2026/android-process-creation/"><![CDATA[<blockquote> <p>Este artículo recorre, con un nivel de detalle considerable, el camino que sigue Android para materializar un proceso de aplicación. Empezaremos en el kernel Linux (la llamada <code class="language-plaintext highlighter-rouge">clone()</code> y <code class="language-plaintext highlighter-rouge">copy_process()</code>), pasaremos por <code class="language-plaintext highlighter-rouge">init</code>, el binario <code class="language-plaintext highlighter-rouge">app_process</code>, la inicialización de <code class="language-plaintext highlighter-rouge">AndroidRuntime</code>, la bifurcación del <strong>Zygote</strong> y terminaremos cuando <code class="language-plaintext highlighter-rouge">ActivityThread.main()</code> empieza a despachar mensajes en el hilo principal de la app. El objetivo es entender no solo “cómo se lanza una app”, sino qué primitivas, decisiones de diseño y consideraciones de seguridad están en juego en cada paso.</p> </blockquote> <h2 id="1-contexto-por-qué-importa-entender-este-flujo">1. Contexto: por qué importa entender este flujo</h2> <p>Android es, en esencia, un Linux con un userland radicalmente distinto al de cualquier distribución GNU/Linux. La capa de aplicación se ejecuta sobre una máquina virtual gestionada (ART) y cada app vive en su propio proceso aislado por UID, SELinux y seccomp. Toda esa arquitectura descansa sobre un truco muy concreto: en lugar de arrancar una nueva instancia de ART desde cero cada vez que el usuario abre una app, Android <strong>clona</strong> un proceso ya inicializado con todas las clases del framework cargadas. Ese proceso plantilla es el <strong>Zygote</strong>.</p> <p>Entender este flujo es valioso para:</p> <ul> <li><strong>Ingeniería de rendimiento</strong>: las optimizaciones de <em>cold start</em>, <em>preload</em>, y <em>class preinit</em> se sitúan justo aquí.</li> <li><strong>Seguridad ofensiva y defensiva</strong>: la superficie de ataque de Zygote (sockets <code class="language-plaintext highlighter-rouge">UNIX</code>, comandos en texto, herencia de descriptores) ha producido múltiples CVEs.</li> <li><strong>Forense y análisis de malware</strong>: distinguir un proceso forkeado legítimamente de uno inyectado vía <code class="language-plaintext highlighter-rouge">app_process</code> “a mano” requiere reconocer estas señales.</li> <li><strong>Diseño de sandboxes</strong>: muchas decisiones (SELinux domain transition, seccomp, capabilities drop) ocurren post-fork y no antes.</li> </ul> <h2 id="2-la-base-fork-clone-y-copy_process-en-el-kernel">2. La base: <code class="language-plaintext highlighter-rouge">fork</code>, <code class="language-plaintext highlighter-rouge">clone</code> y <code class="language-plaintext highlighter-rouge">copy_process</code> en el kernel</h2> <h3 id="21-de-fork2-a-clone2">2.1 De <code class="language-plaintext highlighter-rouge">fork(2)</code> a <code class="language-plaintext highlighter-rouge">clone(2)</code></h3> <p>En Linux moderno <strong><code class="language-plaintext highlighter-rouge">fork()</code> no es una syscall propia</strong>: la libc (Bionic en Android) la implementa sobre <code class="language-plaintext highlighter-rouge">clone()</code>. Bionic, en <code class="language-plaintext highlighter-rouge">bionic/libc/bionic/fork.cpp</code>, hace algo equivalente a:</p> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">int</span> <span class="nf">fork</span><span class="p">(</span><span class="kt">void</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">__bionic_atfork_run_prepare</span><span class="p">();</span>

    <span class="n">pid_t</span> <span class="n">pid</span> <span class="o">=</span> <span class="n">clone</span><span class="p">(</span><span class="n">nullptr</span><span class="p">,</span>
                      <span class="n">nullptr</span><span class="p">,</span>
                      <span class="p">(</span><span class="n">SIGCHLD</span> <span class="o">|</span> <span class="n">CLONE_CHILD_SETTID</span> <span class="o">|</span> <span class="n">CLONE_CHILD_CLEARTID</span><span class="p">),</span>
                      <span class="n">nullptr</span><span class="p">,</span>
                      <span class="n">nullptr</span><span class="p">,</span>
                      <span class="n">nullptr</span><span class="p">,</span>
                      <span class="o">&amp;</span><span class="p">(</span><span class="n">self</span><span class="o">-&gt;</span><span class="n">tid</span><span class="p">));</span>

    <span class="k">if</span> <span class="p">(</span><span class="n">pid</span> <span class="o">==</span> <span class="mi">0</span><span class="p">)</span> <span class="p">{</span>
        <span class="c1">// Child</span>
        <span class="n">__bionic_atfork_run_child</span><span class="p">();</span>
    <span class="p">}</span> <span class="k">else</span> <span class="p">{</span>
        <span class="c1">// Parent</span>
        <span class="n">__bionic_atfork_run_parent</span><span class="p">();</span>
    <span class="p">}</span>
    <span class="k">return</span> <span class="n">pid</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div> <p>Lo importante:</p> <ul> <li><code class="language-plaintext highlighter-rouge">SIGCHLD</code> indica al padre que reciba esa señal cuando el hijo muera (semántica clásica de <code class="language-plaintext highlighter-rouge">fork</code>).</li> <li><code class="language-plaintext highlighter-rouge">CLONE_CHILD_SETTID</code> / <code class="language-plaintext highlighter-rouge">CLONE_CHILD_CLEARTID</code> permiten que el TID del hijo se publique en una dirección de memoria y se limpie al exit; esto es lo que hace que <code class="language-plaintext highlighter-rouge">pthread_join</code>/futex funcionen correctamente.</li> <li><strong>No se pasa ningún flag <code class="language-plaintext highlighter-rouge">CLONE_VM</code>, <code class="language-plaintext highlighter-rouge">CLONE_FS</code>, <code class="language-plaintext highlighter-rouge">CLONE_FILES</code>, <code class="language-plaintext highlighter-rouge">CLONE_SIGHAND</code></strong>: por eso el hijo recibe una copia (copy-on-write) del espacio de direcciones, su propia tabla de descriptores, su propia tabla de señales, etc. Esto es la diferencia entre <code class="language-plaintext highlighter-rouge">fork()</code> y un hilo.</li> </ul> <h3 id="22-lo-que-ocurre-en-copy_process">2.2 Lo que ocurre en <code class="language-plaintext highlighter-rouge">copy_process()</code></h3> <p>Dentro del kernel (<code class="language-plaintext highlighter-rouge">kernel/fork.c</code>), <code class="language-plaintext highlighter-rouge">clone()</code> invoca a <code class="language-plaintext highlighter-rouge">kernel_clone()</code> que llama a <code class="language-plaintext highlighter-rouge">copy_process()</code>. Allí se construye el nuevo <code class="language-plaintext highlighter-rouge">task_struct</code>:</p> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">static</span> <span class="n">__latent_entropy</span> <span class="k">struct</span> <span class="n">task_struct</span> <span class="o">*</span><span class="nf">copy_process</span><span class="p">(</span>
        <span class="k">struct</span> <span class="n">pid</span> <span class="o">*</span><span class="n">pid</span><span class="p">,</span>
        <span class="kt">int</span> <span class="n">trace</span><span class="p">,</span>
        <span class="kt">int</span> <span class="n">node</span><span class="p">,</span>
        <span class="k">struct</span> <span class="n">kernel_clone_args</span> <span class="o">*</span><span class="n">args</span><span class="p">)</span>
<span class="p">{</span>
    <span class="cm">/* 1. dup_task_struct: nuevo task_struct + stack del kernel */</span>
    <span class="n">p</span> <span class="o">=</span> <span class="n">dup_task_struct</span><span class="p">(</span><span class="n">current</span><span class="p">,</span> <span class="n">node</span><span class="p">);</span>

    <span class="cm">/* 2. Comprobaciones de credenciales y rlimit RLIMIT_NPROC */</span>
    <span class="n">retval</span> <span class="o">=</span> <span class="n">copy_creds</span><span class="p">(</span><span class="n">p</span><span class="p">,</span> <span class="n">clone_flags</span><span class="p">);</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">atomic_read</span><span class="p">(</span><span class="o">&amp;</span><span class="n">p</span><span class="o">-&gt;</span><span class="n">real_cred</span><span class="o">-&gt;</span><span class="n">user</span><span class="o">-&gt;</span><span class="n">processes</span><span class="p">)</span> <span class="o">&gt;=</span>
        <span class="n">task_rlimit</span><span class="p">(</span><span class="n">p</span><span class="p">,</span> <span class="n">RLIMIT_NPROC</span><span class="p">))</span> <span class="p">...</span>

    <span class="cm">/* 3. Duplicación de subsistemas según los flags CLONE_* */</span>
    <span class="n">retval</span> <span class="o">=</span> <span class="n">copy_files</span><span class="p">(</span><span class="n">clone_flags</span><span class="p">,</span> <span class="n">p</span><span class="p">);</span>
    <span class="n">retval</span> <span class="o">=</span> <span class="n">copy_fs</span><span class="p">(</span><span class="n">clone_flags</span><span class="p">,</span> <span class="n">p</span><span class="p">);</span>
    <span class="n">retval</span> <span class="o">=</span> <span class="n">copy_sighand</span><span class="p">(</span><span class="n">clone_flags</span><span class="p">,</span> <span class="n">p</span><span class="p">);</span>
    <span class="n">retval</span> <span class="o">=</span> <span class="n">copy_signal</span><span class="p">(</span><span class="n">clone_flags</span><span class="p">,</span> <span class="n">p</span><span class="p">);</span>
    <span class="n">retval</span> <span class="o">=</span> <span class="n">copy_mm</span><span class="p">(</span><span class="n">clone_flags</span><span class="p">,</span> <span class="n">p</span><span class="p">);</span>   <span class="cm">/* COW vía dup_mmap */</span>
    <span class="n">retval</span> <span class="o">=</span> <span class="n">copy_namespaces</span><span class="p">(</span><span class="n">clone_flags</span><span class="p">,</span> <span class="n">p</span><span class="p">);</span>
    <span class="n">retval</span> <span class="o">=</span> <span class="n">copy_io</span><span class="p">(</span><span class="n">clone_flags</span><span class="p">,</span> <span class="n">p</span><span class="p">);</span>
    <span class="n">retval</span> <span class="o">=</span> <span class="n">copy_thread</span><span class="p">(</span><span class="n">clone_flags</span><span class="p">,</span> <span class="n">args</span><span class="o">-&gt;</span><span class="n">stack</span><span class="p">,</span> <span class="p">...);</span>

    <span class="cm">/* 4. Alta en las tablas: pid hash, lista del padre, cgroup */</span>
    <span class="n">attach_pid</span><span class="p">(</span><span class="n">p</span><span class="p">,</span> <span class="n">PIDTYPE_PID</span><span class="p">);</span>
    <span class="p">...</span>
<span class="p">}</span>
</code></pre></div></div> <p>Lo más relevante para Android:</p> <ul> <li><strong><code class="language-plaintext highlighter-rouge">copy_mm()</code> y COW</strong>: el <code class="language-plaintext highlighter-rouge">mm_struct</code> se duplica con <code class="language-plaintext highlighter-rouge">dup_mmap()</code>. Las VMAs anónimas y privadas se marcan <strong>copy-on-write</strong>: las páginas físicas no se replican hasta la primera escritura. Esto es la razón por la que un fork de Zygote (que tiene cientos de MB de heap ART, <code class="language-plaintext highlighter-rouge">boot.art</code>, clases del framework, recursos, etc.) cuesta pocos milisegundos y unos pocos KB iniciales de RAM.</li> <li><strong><code class="language-plaintext highlighter-rouge">copy_files()</code></strong>: el hijo hereda los <code class="language-plaintext highlighter-rouge">fd</code> abiertos del padre. Esto <strong>es un problema de seguridad</strong> que Zygote ataja explícitamente cerrando descriptores antes y validando los que sobreviven.</li> <li><strong><code class="language-plaintext highlighter-rouge">copy_creds()</code></strong>: por defecto el hijo hereda UID/GID; Android sobrescribe estas credenciales post-fork con <code class="language-plaintext highlighter-rouge">setresuid/setresgid</code> para asignar el UID de la app.</li> </ul> <h3 id="23-selinux-capabilities-y-seccomp-también-se-heredan">2.3 SELinux, capabilities y seccomp también se heredan</h3> <p>El kernel propaga al hijo:</p> <ul> <li>El <strong>dominio SELinux</strong> actual (definido por la política, transitable con <code class="language-plaintext highlighter-rouge">setexeccon</code>/<code class="language-plaintext highlighter-rouge">security_bounded_transition</code>).</li> <li>El conjunto de <strong>capabilities</strong> (<code class="language-plaintext highlighter-rouge">cap_effective</code>, <code class="language-plaintext highlighter-rouge">cap_permitted</code>, <code class="language-plaintext highlighter-rouge">cap_inheritable</code>, <code class="language-plaintext highlighter-rouge">cap_bset</code>, <code class="language-plaintext highlighter-rouge">cap_ambient</code>).</li> <li>Los filtros <strong>seccomp-bpf</strong> instalados con <code class="language-plaintext highlighter-rouge">PR_SET_SECCOMP</code> o <code class="language-plaintext highlighter-rouge">seccomp(2)</code>.</li> </ul> <p>Esto significa que cualquier endurecimiento aplicado <strong>antes</strong> del fork se hereda, y cualquier endurecimiento adicional debe aplicarse <strong>después</strong> en el hijo. Zygote utiliza ambos enfoques (algunos seccomp filters están en el Zygote, otros se instalan en el child).</p> <h2 id="3-userspace-del-init-al-zygote">3. Userspace: del <code class="language-plaintext highlighter-rouge">init</code> al Zygote</h2> <h3 id="31-init-arranca-todo">3.1 <code class="language-plaintext highlighter-rouge">init</code> arranca todo</h3> <p><code class="language-plaintext highlighter-rouge">init</code> (PID 1) es el primer proceso de userland. Está implementado en C++ (<code class="language-plaintext highlighter-rouge">system/core/init/</code>) y parsea <code class="language-plaintext highlighter-rouge">init.rc</code>. Una sección relevante de <code class="language-plaintext highlighter-rouge">system/core/rootdir/init.zygote64.rc</code> se ve así:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>service zygote /system/bin/app_process64 <span class="nt">-Xzygote</span> /system/bin <span class="nt">--zygote</span> <span class="nt">--start-system-server</span>
    class main
    priority <span class="nt">-20</span>
    user root
    group root readproc reserved_disk
    socket zygote stream 660 root system
    socket usap_pool_primary stream 660 root system
    onrestart write /sys/android_power/request_state wake
    onrestart restart audioserver
    onrestart restart cameraserver
    onrestart restart media
    onrestart restart netd
    onrestart restart wificond
    writepid /dev/cpuset/foreground/tasks
</code></pre></div></div> <p>Puntos a destacar:</p> <ul> <li><strong><code class="language-plaintext highlighter-rouge">/system/bin/app_process64</code></strong> es el binario que arranca: es ELF nativo, no Dalvik/ART.</li> <li><strong><code class="language-plaintext highlighter-rouge">socket zygote stream 660 root system</code></strong>: <code class="language-plaintext highlighter-rouge">init</code> crea un socket Unix de tipo <code class="language-plaintext highlighter-rouge">SOCK_STREAM</code> en <code class="language-plaintext highlighter-rouge">/dev/socket/zygote</code> antes de hacer <code class="language-plaintext highlighter-rouge">execve</code>, y pasa el <code class="language-plaintext highlighter-rouge">fd</code> ya abierto al proceso via la variable de entorno <code class="language-plaintext highlighter-rouge">ANDROID_SOCKET_zygote</code>. Es a través de ese socket por donde <code class="language-plaintext highlighter-rouge">system_server</code> (y luego el framework) le pedirá nuevos forks.</li> <li><strong><code class="language-plaintext highlighter-rouge">onrestart ...</code></strong>: si Zygote muere, varios servicios críticos se reinician en cascada. Matar al Zygote equivale efectivamente a reiniciar el espacio de usuario.</li> <li><strong><code class="language-plaintext highlighter-rouge">--start-system-server</code></strong>: la primera tarea de Zygote, tras inicializar el runtime, es forkearse para crear <code class="language-plaintext highlighter-rouge">system_server</code>.</li> </ul> <p>En arquitecturas de 64 bits con apps de 32, conviven <code class="language-plaintext highlighter-rouge">zygote64</code> y <code class="language-plaintext highlighter-rouge">zygote</code> (32 bits) o el llamado <code class="language-plaintext highlighter-rouge">zygote64_32</code> en modo dual. Ambos escuchan en sockets distintos.</p> <h3 id="32-anatomía-de-app_process">3.2 Anatomía de <code class="language-plaintext highlighter-rouge">app_process</code></h3> <p><code class="language-plaintext highlighter-rouge">frameworks/base/cmds/app_process/app_main.cpp</code> es el <code class="language-plaintext highlighter-rouge">main()</code> nativo. Simplificado:</p> <div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">int</span> <span class="nf">main</span><span class="p">(</span><span class="kt">int</span> <span class="n">argc</span><span class="p">,</span> <span class="kt">char</span><span class="o">*</span> <span class="k">const</span> <span class="n">argv</span><span class="p">[])</span>
<span class="p">{</span>
    <span class="c1">// ...parseo de flags --zygote, --start-system-server, --application,</span>
    <span class="c1">//    --nice-name=, etc...</span>

    <span class="n">AppRuntime</span> <span class="n">runtime</span><span class="p">(</span><span class="n">argv</span><span class="p">[</span><span class="mi">0</span><span class="p">],</span> <span class="n">computeArgBlockSize</span><span class="p">(</span><span class="n">argc</span><span class="p">,</span> <span class="n">argv</span><span class="p">));</span>
    <span class="n">runtime</span><span class="p">.</span><span class="n">addOption</span><span class="p">(</span><span class="n">strdup</span><span class="p">(</span><span class="n">argv0</span><span class="p">));</span>

    <span class="c1">// Procesar runtime args: -Xzygote, etc.</span>
    <span class="k">for</span> <span class="p">(</span><span class="n">i</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">argc</span><span class="p">;</span> <span class="n">i</span><span class="o">++</span><span class="p">)</span> <span class="p">{</span> <span class="p">...</span> <span class="p">}</span>

    <span class="k">if</span> <span class="p">(</span><span class="n">zygote</span><span class="p">)</span> <span class="p">{</span>
        <span class="n">runtime</span><span class="p">.</span><span class="n">start</span><span class="p">(</span><span class="s">"com.android.internal.os.ZygoteInit"</span><span class="p">,</span>
                      <span class="n">args</span><span class="p">,</span> <span class="n">zygote</span><span class="p">);</span>
    <span class="p">}</span> <span class="k">else</span> <span class="nf">if</span> <span class="p">(</span><span class="n">className</span><span class="p">)</span> <span class="p">{</span>
        <span class="n">runtime</span><span class="p">.</span><span class="n">start</span><span class="p">(</span><span class="s">"com.android.internal.os.RuntimeInit"</span><span class="p">,</span>
                      <span class="n">args</span><span class="p">,</span> <span class="n">zygote</span><span class="p">);</span>
    <span class="p">}</span> <span class="k">else</span> <span class="p">{</span>
        <span class="n">fprintf</span><span class="p">(</span><span class="n">stderr</span><span class="p">,</span> <span class="s">"Error: no class name or --zygote supplied.</span><span class="se">\n</span><span class="s">"</span><span class="p">);</span>
        <span class="n">app_usage</span><span class="p">();</span>
        <span class="n">LOG_ALWAYS_FATAL</span><span class="p">(</span><span class="s">"app_process: no class name or --zygote supplied."</span><span class="p">);</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div> <p>Lo que hace, paso a paso:</p> <ol> <li>Construye un <code class="language-plaintext highlighter-rouge">AppRuntime</code> (subclase de <code class="language-plaintext highlighter-rouge">AndroidRuntime</code>).</li> <li>Parsea los argumentos. <code class="language-plaintext highlighter-rouge">--zygote</code> activa la rama de bootstrap del Zygote; <code class="language-plaintext highlighter-rouge">--application</code> arranca una clase Java arbitraria (modo “ejecutable java”).</li> <li>Llama a <code class="language-plaintext highlighter-rouge">AndroidRuntime::start("com.android.internal.os.ZygoteInit", ...)</code>.</li> </ol> <p><code class="language-plaintext highlighter-rouge">app_process</code> es también el binario que utilizan cosas como <code class="language-plaintext highlighter-rouge">am</code>, <code class="language-plaintext highlighter-rouge">pm</code>, herramientas de debug y, notablemente, ataques o pruebas de inyección donde se quiere lanzar código Java arbitrario con una identidad determinada. Por eso ver <code class="language-plaintext highlighter-rouge">app_process</code> ejecutándose con argumentos no estándar es siempre una señal a vigilar.</p> <h3 id="33-androidruntimestart-crea-la-vm">3.3 <code class="language-plaintext highlighter-rouge">AndroidRuntime::start</code> crea la VM</h3> <p><code class="language-plaintext highlighter-rouge">frameworks/base/core/jni/AndroidRuntime.cpp</code>:</p> <div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">void</span> <span class="n">AndroidRuntime</span><span class="o">::</span><span class="n">start</span><span class="p">(</span><span class="k">const</span> <span class="kt">char</span><span class="o">*</span> <span class="n">className</span><span class="p">,</span> <span class="k">const</span> <span class="n">Vector</span><span class="o">&lt;</span><span class="n">String8</span><span class="o">&gt;&amp;</span> <span class="n">options</span><span class="p">,</span> <span class="kt">bool</span> <span class="n">zygote</span><span class="p">)</span>
<span class="p">{</span>
    <span class="c1">// 1. Inicializar JNI invocando JNI_CreateJavaVM con opciones ART.</span>
    <span class="n">JNIEnv</span><span class="o">*</span> <span class="n">env</span><span class="p">;</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">startVm</span><span class="p">(</span><span class="o">&amp;</span><span class="n">mJavaVM</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">env</span><span class="p">,</span> <span class="n">zygote</span><span class="p">,</span> <span class="n">primary_zygote</span><span class="p">)</span> <span class="o">!=</span> <span class="mi">0</span><span class="p">)</span> <span class="p">{</span>
        <span class="k">return</span><span class="p">;</span>
    <span class="p">}</span>
    <span class="n">onVmCreated</span><span class="p">(</span><span class="n">env</span><span class="p">);</span>

    <span class="c1">// 2. Registrar todas las funciones JNI del framework (cientos).</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">startReg</span><span class="p">(</span><span class="n">env</span><span class="p">)</span> <span class="o">&lt;</span> <span class="mi">0</span><span class="p">)</span> <span class="p">{</span> <span class="p">...</span> <span class="p">}</span>

    <span class="c1">// 3. Localizar la clase Java de entrada (ZygoteInit o RuntimeInit).</span>
    <span class="kt">char</span><span class="o">*</span> <span class="n">slashClassName</span> <span class="o">=</span> <span class="n">toSlashClassName</span><span class="p">(</span><span class="n">className</span><span class="p">);</span>
    <span class="n">jclass</span> <span class="n">startClass</span> <span class="o">=</span> <span class="n">env</span><span class="o">-&gt;</span><span class="n">FindClass</span><span class="p">(</span><span class="n">slashClassName</span><span class="p">);</span>

    <span class="c1">// 4. Localizar el método estático main([Ljava/lang/String;)V</span>
    <span class="n">jmethodID</span> <span class="n">startMeth</span> <span class="o">=</span> <span class="n">env</span><span class="o">-&gt;</span><span class="n">GetStaticMethodID</span><span class="p">(</span><span class="n">startClass</span><span class="p">,</span> <span class="s">"main"</span><span class="p">,</span>
            <span class="s">"([Ljava/lang/String;)V"</span><span class="p">);</span>

    <span class="c1">// 5. Invocarlo. Aquí cedemos el control al mundo Java.</span>
    <span class="n">env</span><span class="o">-&gt;</span><span class="n">CallStaticVoidMethod</span><span class="p">(</span><span class="n">startClass</span><span class="p">,</span> <span class="n">startMeth</span><span class="p">,</span> <span class="n">strArray</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div> <p>Cosas clave:</p> <ul> <li><strong><code class="language-plaintext highlighter-rouge">startVm</code></strong> decide los argumentos para <code class="language-plaintext highlighter-rouge">JNI_CreateJavaVM</code>: tamaño de heap, image path (<code class="language-plaintext highlighter-rouge">boot.art</code>), modo JIT, opciones de GC, debug, etc. Algunas opciones son distintas según <em>es Zygote</em> o no (por ejemplo, el GC se comporta diferente para favorecer el COW).</li> <li><strong><code class="language-plaintext highlighter-rouge">startReg</code></strong> invoca <code class="language-plaintext highlighter-rouge">register_jni_procs()</code>, que registra una tabla enorme de funciones nativas. Esto es lo que permite que <code class="language-plaintext highlighter-rouge">Surface</code>, <code class="language-plaintext highlighter-rouge">Binder</code>, <code class="language-plaintext highlighter-rouge">ParcelFileDescriptor</code>, etc. tengan implementación nativa.</li> <li>A partir del <code class="language-plaintext highlighter-rouge">CallStaticVoidMethod</code>, <strong>estamos en Java dentro del Zygote</strong>.</li> </ul> <h2 id="4-el-zygote-en-java-zygoteinitmain">4. El Zygote en Java: <code class="language-plaintext highlighter-rouge">ZygoteInit.main()</code></h2> <p>El fichero clave es <code class="language-plaintext highlighter-rouge">frameworks/base/core/java/com/android/internal/os/ZygoteInit.java</code>. Su <code class="language-plaintext highlighter-rouge">main()</code> realiza la inicialización pesada <strong>una sola vez</strong> para que todos los hijos hereden el trabajo ya hecho.</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">static</span> <span class="kt">void</span> <span class="nf">main</span><span class="o">(</span><span class="nc">String</span><span class="o">[]</span> <span class="n">argv</span><span class="o">)</span> <span class="o">{</span>
    <span class="nc">ZygoteServer</span> <span class="n">zygoteServer</span> <span class="o">=</span> <span class="kc">null</span><span class="o">;</span>
    <span class="k">try</span> <span class="o">{</span>
        <span class="nc">Trace</span><span class="o">.</span><span class="na">traceBegin</span><span class="o">(</span><span class="nc">Trace</span><span class="o">.</span><span class="na">TRACE_TAG_DALVIK</span><span class="o">,</span> <span class="s">"ZygoteInit"</span><span class="o">);</span>

        <span class="nc">RuntimeInit</span><span class="o">.</span><span class="na">preForkInit</span><span class="o">();</span>

        <span class="c1">// Bootclasspath y librerías</span>
        <span class="n">bootTimingsTraceLog</span><span class="o">.</span><span class="na">traceBegin</span><span class="o">(</span><span class="s">"ZygoteInit"</span><span class="o">);</span>
        <span class="nc">ZygoteInit</span><span class="o">.</span><span class="na">preload</span><span class="o">(</span><span class="n">bootTimingsTraceLog</span><span class="o">);</span>

        <span class="c1">// GC explícito y heap trim: deja la imagen "limpia"</span>
        <span class="c1">// antes de empezar a forkear.</span>
        <span class="n">gcAndFinalize</span><span class="o">();</span>

        <span class="nc">Zygote</span><span class="o">.</span><span class="na">initNativeState</span><span class="o">(</span><span class="n">isPrimaryZygote</span><span class="o">);</span>

        <span class="nc">ZygoteHooks</span><span class="o">.</span><span class="na">stopZygoteNoThreadCreation</span><span class="o">();</span>

        <span class="n">zygoteServer</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">ZygoteServer</span><span class="o">(</span><span class="n">isPrimaryZygote</span><span class="o">);</span>

        <span class="k">if</span> <span class="o">(</span><span class="n">startSystemServer</span><span class="o">)</span> <span class="o">{</span>
            <span class="nc">Runnable</span> <span class="n">r</span> <span class="o">=</span> <span class="n">forkSystemServer</span><span class="o">(</span><span class="n">abiList</span><span class="o">,</span> <span class="n">zygoteSocketName</span><span class="o">,</span> <span class="n">zygoteServer</span><span class="o">);</span>
            <span class="k">if</span> <span class="o">(</span><span class="n">r</span> <span class="o">!=</span> <span class="kc">null</span><span class="o">)</span> <span class="o">{</span>
                <span class="n">r</span><span class="o">.</span><span class="na">run</span><span class="o">();</span>
                <span class="k">return</span><span class="o">;</span>
            <span class="o">}</span>
        <span class="o">}</span>

        <span class="c1">// Bucle de servidor: aceptar conexiones del socket y forkear.</span>
        <span class="n">caller</span> <span class="o">=</span> <span class="n">zygoteServer</span><span class="o">.</span><span class="na">runSelectLoop</span><span class="o">(</span><span class="n">abiList</span><span class="o">);</span>
    <span class="o">}</span> <span class="k">finally</span> <span class="o">{</span>
        <span class="k">if</span> <span class="o">(</span><span class="n">zygoteServer</span> <span class="o">!=</span> <span class="kc">null</span><span class="o">)</span> <span class="n">zygoteServer</span><span class="o">.</span><span class="na">closeServerSocket</span><span class="o">();</span>
    <span class="o">}</span>

    <span class="c1">// Si llegamos aquí, somos un proceso forkeado al que</span>
    <span class="c1">// runSelectLoop le ha devuelto un Runnable. Lo ejecutamos.</span>
    <span class="k">if</span> <span class="o">(</span><span class="n">caller</span> <span class="o">!=</span> <span class="kc">null</span><span class="o">)</span> <span class="n">caller</span><span class="o">.</span><span class="na">run</span><span class="o">();</span>
<span class="o">}</span>
</code></pre></div></div> <p>Hay varias fases distintas que conviene desgranar.</p> <h3 id="41-preload-el-corazón-del-modelo-zygote">4.1 <code class="language-plaintext highlighter-rouge">preload()</code>: el corazón del modelo Zygote</h3> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">static</span> <span class="kt">void</span> <span class="nf">preload</span><span class="o">(</span><span class="nc">TimingsTraceLog</span> <span class="n">bootTimingsTraceLog</span><span class="o">)</span> <span class="o">{</span>
    <span class="n">beginIcuCachePinning</span><span class="o">();</span>
    <span class="n">bootTimingsTraceLog</span><span class="o">.</span><span class="na">traceBegin</span><span class="o">(</span><span class="s">"PreloadClasses"</span><span class="o">);</span>
    <span class="n">preloadClasses</span><span class="o">();</span>
    <span class="n">bootTimingsTraceLog</span><span class="o">.</span><span class="na">traceEnd</span><span class="o">();</span>

    <span class="n">bootTimingsTraceLog</span><span class="o">.</span><span class="na">traceBegin</span><span class="o">(</span><span class="s">"CacheNonBootClasspathClassLoaders"</span><span class="o">);</span>
    <span class="n">cacheNonBootClasspathClassLoaders</span><span class="o">();</span>
    <span class="n">bootTimingsTraceLog</span><span class="o">.</span><span class="na">traceEnd</span><span class="o">();</span>

    <span class="n">bootTimingsTraceLog</span><span class="o">.</span><span class="na">traceBegin</span><span class="o">(</span><span class="s">"PreloadResources"</span><span class="o">);</span>
    <span class="nc">Resources</span><span class="o">.</span><span class="na">preloadResources</span><span class="o">();</span>
    <span class="n">bootTimingsTraceLog</span><span class="o">.</span><span class="na">traceEnd</span><span class="o">();</span>

    <span class="n">nativePreloadAppProcessHALs</span><span class="o">();</span>
    <span class="n">maybePreloadGraphicsDriver</span><span class="o">();</span>
    <span class="n">preloadSharedLibraries</span><span class="o">();</span>
    <span class="n">preloadTextResources</span><span class="o">();</span>
    <span class="nc">WebViewFactory</span><span class="o">.</span><span class="na">prepareWebViewInZygote</span><span class="o">();</span>

    <span class="n">endIcuCachePinning</span><span class="o">();</span>
    <span class="n">warmUpJcaProviders</span><span class="o">();</span>
    <span class="n">sPreloadComplete</span> <span class="o">=</span> <span class="kc">true</span><span class="o">;</span>
<span class="o">}</span>
</code></pre></div></div> <ul> <li><strong><code class="language-plaintext highlighter-rouge">preloadClasses()</code></strong> lee <code class="language-plaintext highlighter-rouge">/system/etc/preloaded-classes</code> (varios miles de nombres totalmente cualificados) y las inicializa con <code class="language-plaintext highlighter-rouge">Class.forName(name, true, classLoader)</code>. El <code class="language-plaintext highlighter-rouge">initialize=true</code> ejecuta los <code class="language-plaintext highlighter-rouge">&lt;clinit&gt;</code> estáticos, lo que llena los pools y caches del framework. Coste: ~30–60 segundos en el primer boot. Beneficio: cada app forkeada arranca con todo eso “gratis” gracias al COW.</li> <li><strong><code class="language-plaintext highlighter-rouge">Resources.preloadResources()</code></strong> carga drawables y atributos comunes del <code class="language-plaintext highlighter-rouge">framework-res.apk</code>.</li> <li><strong><code class="language-plaintext highlighter-rouge">WebViewFactory.prepareWebViewInZygote()</code></strong> mapea y enlaza la WebView seleccionada en el Zygote para que sus apps la compartan sin pagar otra vez la carga.</li> <li><strong><code class="language-plaintext highlighter-rouge">preloadSharedLibraries()</code></strong> hace <code class="language-plaintext highlighter-rouge">System.loadLibrary</code> de <code class="language-plaintext highlighter-rouge">android</code>, <code class="language-plaintext highlighter-rouge">compiler_rt</code>, <code class="language-plaintext highlighter-rouge">jnigraphics</code>, etc.</li> </ul> <blockquote> <p><strong>Nota de rendimiento</strong>: cualquier clase que <strong>no</strong> esté en <code class="language-plaintext highlighter-rouge">preloaded-classes</code> y que la app cargue en arranque, paga JIT/PLT/relocaciones por su cuenta. Los OEMs ajustan este fichero para reducir <em>cold start</em>.</p> </blockquote> <h3 id="42-gcandfinalize-y-la-magia-del-cow">4.2 <code class="language-plaintext highlighter-rouge">gcAndFinalize()</code> y la magia del COW</h3> <p>Después del <code class="language-plaintext highlighter-rouge">preload</code>, el Zygote ejecuta:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">static</span> <span class="kt">void</span> <span class="nf">gcAndFinalize</span><span class="o">()</span> <span class="o">{</span>
    <span class="nc">Runtime</span> <span class="n">runtime</span> <span class="o">=</span> <span class="nc">Runtime</span><span class="o">.</span><span class="na">getRuntime</span><span class="o">();</span>
    <span class="nc">System</span><span class="o">.</span><span class="na">runFinalization</span><span class="o">();</span>
    <span class="n">runtime</span><span class="o">.</span><span class="na">gc</span><span class="o">();</span>
    <span class="nc">System</span><span class="o">.</span><span class="na">runFinalization</span><span class="o">();</span>
    <span class="n">runtime</span><span class="o">.</span><span class="na">gc</span><span class="o">();</span>
<span class="o">}</span>
</code></pre></div></div> <p>El objetivo es <strong>dejar el heap lo más compacto y “tocado” posible</strong> justo antes de empezar a forkear. ART implementa el GC con la compactación pensada para esto: si el heap está bien empaquetado, los hijos comparten páginas físicas durante mucho más tiempo y la métrica <code class="language-plaintext highlighter-rouge">PSS</code> (Proportional Set Size) por app se mantiene baja.</p> <h3 id="43-zygoteserver-socket--select-loop">4.3 <code class="language-plaintext highlighter-rouge">ZygoteServer</code>: socket + select-loop</h3> <p><code class="language-plaintext highlighter-rouge">ZygoteServer</code> recupera el <code class="language-plaintext highlighter-rouge">fd</code> heredado de <code class="language-plaintext highlighter-rouge">init</code> desde la variable <code class="language-plaintext highlighter-rouge">ANDROID_SOCKET_zygote</code> y se sitúa a escuchar:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">ZygoteServer</span><span class="o">(</span><span class="kt">boolean</span> <span class="n">isPrimaryZygote</span><span class="o">)</span> <span class="o">{</span>
    <span class="n">mUsapPoolEventFd</span> <span class="o">=</span> <span class="nc">Zygote</span><span class="o">.</span><span class="na">getUsapPoolEventFD</span><span class="o">();</span>
    <span class="k">if</span> <span class="o">(</span><span class="n">isPrimaryZygote</span><span class="o">)</span> <span class="o">{</span>
        <span class="n">mZygoteSocket</span> <span class="o">=</span> <span class="nc">Zygote</span><span class="o">.</span><span class="na">createManagedSocketFromInitSocket</span><span class="o">(</span>
                <span class="nc">Zygote</span><span class="o">.</span><span class="na">PRIMARY_SOCKET_NAME</span><span class="o">);</span>   <span class="c1">// "zygote"</span>
        <span class="n">mUsapPoolSocket</span> <span class="o">=</span> <span class="nc">Zygote</span><span class="o">.</span><span class="na">createManagedSocketFromInitSocket</span><span class="o">(</span>
                <span class="nc">Zygote</span><span class="o">.</span><span class="na">USAP_POOL_PRIMARY_SOCKET_NAME</span><span class="o">);</span>
    <span class="o">}</span> <span class="k">else</span> <span class="o">{</span>
        <span class="n">mZygoteSocket</span> <span class="o">=</span> <span class="nc">Zygote</span><span class="o">.</span><span class="na">createManagedSocketFromInitSocket</span><span class="o">(</span>
                <span class="nc">Zygote</span><span class="o">.</span><span class="na">SECONDARY_SOCKET_NAME</span><span class="o">);</span> <span class="c1">// "zygote_secondary"</span>
        <span class="o">...</span>
    <span class="o">}</span>
    <span class="n">fetchUsapPoolPolicyProps</span><span class="o">();</span>
<span class="o">}</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">runSelectLoop</code> se queda en un <code class="language-plaintext highlighter-rouge">Os.poll()</code> sobre tres tipos de descriptores:</p> <ol> <li>El socket de servidor: nuevas conexiones (típicamente desde <code class="language-plaintext highlighter-rouge">system_server</code>).</li> <li>Las conexiones aceptadas: comandos <code class="language-plaintext highlighter-rouge">--fork-child</code>, <code class="language-plaintext highlighter-rouge">--query-abi-list</code>, etc.</li> <li>El <code class="language-plaintext highlighter-rouge">eventfd</code> que controla el <em>USAP pool</em>.</li> </ol> <p>Cuando llega un comando, lo deserializa con <code class="language-plaintext highlighter-rouge">ZygoteArguments</code> (una bolsa de flags como <code class="language-plaintext highlighter-rouge">--setuid=</code>, <code class="language-plaintext highlighter-rouge">--setgid=</code>, <code class="language-plaintext highlighter-rouge">--target-sdk-version=</code>, <code class="language-plaintext highlighter-rouge">--nice-name=</code>, <code class="language-plaintext highlighter-rouge">--seinfo=</code>, <code class="language-plaintext highlighter-rouge">--instruction-set=</code>) y dispara <code class="language-plaintext highlighter-rouge">Zygote.forkAndSpecialize(...)</code>.</p> <h3 id="44-usap-el-unspecialized-app-process-pool">4.4 USAP: el <em>Unspecialized App Process</em> pool</h3> <p>A partir de Android 10 existe el <em>USAP pool</em>: un conjunto de procesos hijos del Zygote <strong>ya forkeados pero todavía sin especializar</strong> (sin UID, sin SELinux domain final, sin seccomp final). Cuando llega una petición de arranque, el Zygote elige uno del pool y le envía la “especialización” por su socket. Esto evita el <code class="language-plaintext highlighter-rouge">fork()</code> en la ruta crítica de <em>cold start</em>.</p> <p>El pool se gestiona con un <code class="language-plaintext highlighter-rouge">eventfd</code> que se señaliza cuando hay que rellenar; los USAP esperan en un <code class="language-plaintext highlighter-rouge">read()</code> bloqueante sobre su socket personal.</p> <h2 id="5-el-fork-especializado-zygoteforkandspecialize">5. El fork especializado: <code class="language-plaintext highlighter-rouge">Zygote.forkAndSpecialize</code></h2> <p>El método <code class="language-plaintext highlighter-rouge">Zygote.forkAndSpecialize</code> es la puerta entre el mundo “soy el Zygote” y el mundo “soy una app concreta”. Su firma es enorme porque recibe absolutamente todo lo necesario para configurar el hijo antes de que ejecute código de aplicación:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">static</span> <span class="kt">int</span> <span class="nf">forkAndSpecialize</span><span class="o">(</span><span class="kt">int</span> <span class="n">uid</span><span class="o">,</span> <span class="kt">int</span> <span class="n">gid</span><span class="o">,</span> <span class="kt">int</span><span class="o">[]</span> <span class="n">gids</span><span class="o">,</span>
        <span class="kt">int</span> <span class="n">runtimeFlags</span><span class="o">,</span> <span class="kt">int</span><span class="o">[][]</span> <span class="n">rlimits</span><span class="o">,</span> <span class="kt">int</span> <span class="n">mountExternal</span><span class="o">,</span>
        <span class="nc">String</span> <span class="n">seInfo</span><span class="o">,</span> <span class="nc">String</span> <span class="n">niceName</span><span class="o">,</span> <span class="kt">int</span><span class="o">[]</span> <span class="n">fdsToClose</span><span class="o">,</span> <span class="kt">int</span><span class="o">[]</span> <span class="n">fdsToIgnore</span><span class="o">,</span>
        <span class="kt">boolean</span> <span class="n">startChildZygote</span><span class="o">,</span> <span class="nc">String</span> <span class="n">instructionSet</span><span class="o">,</span> <span class="nc">String</span> <span class="n">appDataDir</span><span class="o">,</span>
        <span class="kt">boolean</span> <span class="n">isTopApp</span><span class="o">,</span> <span class="nc">String</span><span class="o">[]</span> <span class="n">pkgDataInfoList</span><span class="o">,</span>
        <span class="nc">String</span><span class="o">[]</span> <span class="n">allowlistedDataInfoList</span><span class="o">,</span> <span class="kt">boolean</span> <span class="n">bindMountAppDataDirs</span><span class="o">,</span>
        <span class="kt">boolean</span> <span class="n">bindMountAppStorageDirs</span><span class="o">)</span> <span class="o">{</span>

    <span class="nc">ZygoteHooks</span><span class="o">.</span><span class="na">preFork</span><span class="o">();</span>

    <span class="kt">int</span> <span class="n">pid</span> <span class="o">=</span> <span class="n">nativeForkAndSpecialize</span><span class="o">(</span><span class="n">uid</span><span class="o">,</span> <span class="n">gid</span><span class="o">,</span> <span class="n">gids</span><span class="o">,</span> <span class="n">runtimeFlags</span><span class="o">,</span> <span class="n">rlimits</span><span class="o">,</span>
            <span class="n">mountExternal</span><span class="o">,</span> <span class="n">seInfo</span><span class="o">,</span> <span class="n">niceName</span><span class="o">,</span> <span class="n">fdsToClose</span><span class="o">,</span> <span class="n">fdsToIgnore</span><span class="o">,</span>
            <span class="n">startChildZygote</span><span class="o">,</span> <span class="n">instructionSet</span><span class="o">,</span> <span class="n">appDataDir</span><span class="o">,</span> <span class="n">isTopApp</span><span class="o">,</span>
            <span class="n">pkgDataInfoList</span><span class="o">,</span> <span class="n">allowlistedDataInfoList</span><span class="o">,</span>
            <span class="n">bindMountAppDataDirs</span><span class="o">,</span> <span class="n">bindMountAppStorageDirs</span><span class="o">);</span>

    <span class="k">if</span> <span class="o">(</span><span class="n">pid</span> <span class="o">==</span> <span class="mi">0</span><span class="o">)</span> <span class="o">{</span>
        <span class="nc">Trace</span><span class="o">.</span><span class="na">setTracingEnabled</span><span class="o">(</span><span class="kc">true</span><span class="o">,</span> <span class="n">runtimeFlags</span><span class="o">);</span>
        <span class="nc">Trace</span><span class="o">.</span><span class="na">traceBegin</span><span class="o">(</span><span class="nc">Trace</span><span class="o">.</span><span class="na">TRACE_TAG_ACTIVITY_MANAGER</span><span class="o">,</span> <span class="s">"PostFork"</span><span class="o">);</span>
    <span class="o">}</span> <span class="k">else</span> <span class="o">{</span>
        <span class="nc">Trace</span><span class="o">.</span><span class="na">setTracingEnabled</span><span class="o">(</span><span class="kc">false</span><span class="o">,</span> <span class="mi">0</span><span class="o">);</span>
    <span class="o">}</span>

    <span class="nc">ZygoteHooks</span><span class="o">.</span><span class="na">postForkCommon</span><span class="o">();</span>
    <span class="k">return</span> <span class="n">pid</span><span class="o">;</span>
<span class="o">}</span>
</code></pre></div></div> <h3 id="51-el-descenso-a-jni-com_android_internal_os_zygotecpp">5.1 El descenso a JNI: <code class="language-plaintext highlighter-rouge">com_android_internal_os_Zygote.cpp</code></h3> <p>El <code class="language-plaintext highlighter-rouge">nativeForkAndSpecialize</code> salta a C++ en <code class="language-plaintext highlighter-rouge">frameworks/base/core/jni/com_android_internal_os_Zygote.cpp</code>. La estructura, simplificada, es:</p> <div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">static</span> <span class="n">pid_t</span> <span class="nf">ForkCommon</span><span class="p">(</span><span class="n">JNIEnv</span><span class="o">*</span> <span class="n">env</span><span class="p">,</span> <span class="kt">bool</span> <span class="n">is_system_server</span><span class="p">,</span>
                        <span class="k">const</span> <span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="kt">int</span><span class="o">&gt;&amp;</span> <span class="n">fds_to_close</span><span class="p">,</span>
                        <span class="k">const</span> <span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="kt">int</span><span class="o">&gt;&amp;</span> <span class="n">fds_to_ignore</span><span class="p">,</span>
                        <span class="kt">bool</span> <span class="n">is_priority_fork</span><span class="p">)</span> <span class="p">{</span>

    <span class="n">SetSignalHandlers</span><span class="p">();</span>                              <span class="c1">// (1)</span>

    <span class="c1">// Bloquear SIGCHLD durante el fork</span>
    <span class="n">BlockSignal</span><span class="p">(</span><span class="n">SIGCHLD</span><span class="p">,</span> <span class="n">fail_fn</span><span class="p">);</span>

    <span class="n">__android_log_close</span><span class="p">();</span>                            <span class="c1">// (2)</span>
    <span class="n">AStatsSocket_close</span><span class="p">();</span>
    <span class="n">stats_log_close</span><span class="p">();</span>

    <span class="c1">// Cerrar los fds que NO deben heredarse, validar el resto</span>
    <span class="n">FileDescriptorAllowlist</span><span class="o">::</span><span class="n">Get</span><span class="p">()</span><span class="o">-&gt;</span><span class="n">Allow</span><span class="p">(</span><span class="n">fds_to_ignore</span><span class="p">);</span>
    <span class="n">fds_to_close_table</span><span class="p">.</span><span class="n">Restat</span><span class="p">();</span>                      <span class="c1">// (3)</span>

    <span class="n">pid_t</span> <span class="n">pid</span> <span class="o">=</span> <span class="n">fork</span><span class="p">();</span>

    <span class="k">if</span> <span class="p">(</span><span class="n">pid</span> <span class="o">==</span> <span class="mi">0</span><span class="p">)</span> <span class="p">{</span>
        <span class="c1">// ---------- HIJO ----------</span>
        <span class="k">if</span> <span class="p">(</span><span class="n">is_priority_fork</span><span class="p">)</span> <span class="p">{</span>
            <span class="n">setpriority</span><span class="p">(</span><span class="n">PRIO_PROCESS</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="n">PROCESS_PRIORITY_MAX</span><span class="p">);</span>
        <span class="p">}</span>
        <span class="n">PreApplicationInit</span><span class="p">();</span>
    <span class="p">}</span> <span class="k">else</span> <span class="p">{</span>
        <span class="c1">// ---------- PADRE ----------</span>
        <span class="n">UnblockSignal</span><span class="p">(</span><span class="n">SIGCHLD</span><span class="p">,</span> <span class="n">fail_fn</span><span class="p">);</span>
    <span class="p">}</span>
    <span class="k">return</span> <span class="n">pid</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div> <p>Notas:</p> <ol> <li><strong><code class="language-plaintext highlighter-rouge">SetSignalHandlers</code></strong> reinstala manejadores; entre otras cosas un handler de <code class="language-plaintext highlighter-rouge">SIGCHLD</code> que abortar limpia si un hijo muere antes de especializarse.</li> <li><strong>Cerrar logs, statsd, etc.</strong>: evita que dos procesos compartan descriptores de log y se pisen.</li> <li><strong><code class="language-plaintext highlighter-rouge">FileDescriptorTable</code></strong> (<code class="language-plaintext highlighter-rouge">bionic</code> + framework) lleva un registro de <strong>qué <code class="language-plaintext highlighter-rouge">fd</code> están permitidos</strong> en el Zygote (sockets de log allowlisted, <code class="language-plaintext highlighter-rouge">/dev/null</code>, fonts, etc.). Cualquier <code class="language-plaintext highlighter-rouge">fd</code> no allowlisted que sobreviva al fork <strong>es un error</strong> y aborta el proceso. Este mecanismo cierra un vector clásico de fuga de privilegios.</li> </ol> <p>Después de <code class="language-plaintext highlighter-rouge">ForkCommon</code>, en el hijo se ejecuta <code class="language-plaintext highlighter-rouge">SpecializeCommon</code>:</p> <div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">static</span> <span class="kt">void</span> <span class="nf">SpecializeCommon</span><span class="p">(</span><span class="n">JNIEnv</span><span class="o">*</span> <span class="n">env</span><span class="p">,</span> <span class="n">uid_t</span> <span class="n">uid</span><span class="p">,</span> <span class="n">gid_t</span> <span class="n">gid</span><span class="p">,</span>
        <span class="n">jintArray</span> <span class="n">gids</span><span class="p">,</span> <span class="n">jint</span> <span class="n">runtime_flags</span><span class="p">,</span> <span class="n">jobjectArray</span> <span class="n">rlimits</span><span class="p">,</span>
        <span class="n">jlong</span> <span class="n">permitted_capabilities</span><span class="p">,</span> <span class="n">jlong</span> <span class="n">effective_capabilities</span><span class="p">,</span>
        <span class="n">jint</span> <span class="n">mount_external</span><span class="p">,</span> <span class="n">jstring</span> <span class="n">managed_se_info</span><span class="p">,</span>
        <span class="n">jstring</span> <span class="n">managed_nice_name</span><span class="p">,</span> <span class="kt">bool</span> <span class="n">is_system_server</span><span class="p">,</span>
        <span class="kt">bool</span> <span class="n">is_child_zygote</span><span class="p">,</span> <span class="n">jstring</span> <span class="n">managed_instruction_set</span><span class="p">,</span>
        <span class="n">jstring</span> <span class="n">managed_app_data_dir</span><span class="p">,</span> <span class="kt">bool</span> <span class="n">is_top_app</span><span class="p">,</span>
        <span class="n">jobjectArray</span> <span class="n">pkg_data_info_list</span><span class="p">,</span> <span class="p">...)</span> <span class="p">{</span>

    <span class="c1">// (a) Mount namespace y vista del almacenamiento externo.</span>
    <span class="n">MountEmulatedStorage</span><span class="p">(</span><span class="n">uid</span><span class="p">,</span> <span class="n">mount_external</span><span class="p">,</span> <span class="p">...);</span>

    <span class="c1">// (b) Configurar UID/GID. ¡Esto es lo que convierte al proceso</span>
    <span class="c1">//     en "la app". No vuelve atrás.</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">setresgid</span><span class="p">(</span><span class="n">gid</span><span class="p">,</span> <span class="n">gid</span><span class="p">,</span> <span class="n">gid</span><span class="p">)</span> <span class="o">==</span> <span class="o">-</span><span class="mi">1</span><span class="p">)</span> <span class="n">fail_fn</span><span class="p">(...);</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">setgroups</span><span class="p">(</span><span class="n">gids_size</span><span class="p">,</span> <span class="n">gids_data</span><span class="p">)</span> <span class="o">==</span> <span class="o">-</span><span class="mi">1</span><span class="p">)</span> <span class="n">fail_fn</span><span class="p">(...);</span>
    <span class="n">SetInheritable</span><span class="p">(</span><span class="n">permitted_capabilities</span><span class="p">,</span> <span class="n">fail_fn</span><span class="p">);</span>
    <span class="n">DropCapabilitiesBoundingSet</span><span class="p">(</span><span class="n">fail_fn</span><span class="p">);</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">setresuid</span><span class="p">(</span><span class="n">uid</span><span class="p">,</span> <span class="n">uid</span><span class="p">,</span> <span class="n">uid</span><span class="p">)</span> <span class="o">==</span> <span class="o">-</span><span class="mi">1</span><span class="p">)</span> <span class="n">fail_fn</span><span class="p">(...);</span>

    <span class="c1">// (c) Capabilities efectivas finales.</span>
    <span class="n">SetCapabilities</span><span class="p">(</span><span class="n">permitted_capabilities</span><span class="p">,</span> <span class="n">effective_capabilities</span><span class="p">,</span>
                    <span class="n">permitted_capabilities</span><span class="p">,</span> <span class="n">fail_fn</span><span class="p">);</span>

    <span class="c1">// (d) Aplicar seccomp.</span>
    <span class="n">SetSchedulerPolicy</span><span class="p">(</span><span class="n">fail_fn</span><span class="p">,</span> <span class="n">is_top_app</span><span class="p">);</span>
    <span class="n">SetUpSeccompFilter</span><span class="p">(</span><span class="n">uid</span><span class="p">,</span> <span class="n">is_child_zygote</span><span class="p">);</span>

    <span class="c1">// (e) Transición de dominio SELinux.</span>
    <span class="n">rc</span> <span class="o">=</span> <span class="n">selinux_android_setcontext</span><span class="p">(</span><span class="n">uid</span><span class="p">,</span> <span class="n">is_system_server</span><span class="p">,</span> <span class="n">se_info_c_str</span><span class="p">,</span>
                                    <span class="n">nice_name_c_str</span><span class="p">);</span>

    <span class="c1">// (f) Nombre del proceso (visible en /proc/&lt;pid&gt;/comm).</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">nice_name</span><span class="p">.</span><span class="n">has_value</span><span class="p">())</span> <span class="n">SetThreadName</span><span class="p">(</span><span class="n">nice_name</span><span class="p">.</span><span class="n">value</span><span class="p">());</span>

    <span class="c1">// (g) Re-inicializar pseudo-aleatoriedad, OpenSSL, etc.</span>
    <span class="n">__android_log_close</span><span class="p">();</span>
    <span class="n">AndroidLogger_OnLogChanged</span><span class="p">();</span>
    <span class="n">stats_log_init</span><span class="p">();</span>

    <span class="c1">// (h) Hooks Java post-fork (resetear caches, hilos de finalizer, GC).</span>
    <span class="n">env</span><span class="o">-&gt;</span><span class="n">CallStaticVoidMethod</span><span class="p">(</span><span class="n">gZygoteClass</span><span class="p">,</span> <span class="n">gCallPostForkChildHooks</span><span class="p">,</span>
            <span class="n">runtime_flags</span><span class="p">,</span> <span class="n">is_system_server</span><span class="p">,</span> <span class="n">is_child_zygote</span><span class="p">,</span> <span class="n">instruction_set</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div> <p>El orden importa muchísimo. En particular:</p> <ul> <li><code class="language-plaintext highlighter-rouge">setresgid</code>, <code class="language-plaintext highlighter-rouge">setgroups</code>, <code class="language-plaintext highlighter-rouge">setresuid</code> se hacen <strong>después</strong> de configurar capabilities permitidas heredables, porque <code class="language-plaintext highlighter-rouge">setresuid</code> desde root con UID no-cero puede limpiar el conjunto efectivo si no se ha preparado <code class="language-plaintext highlighter-rouge">PR_SET_KEEPCAPS</code> correctamente.</li> <li><code class="language-plaintext highlighter-rouge">selinux_android_setcontext</code> aplica <code class="language-plaintext highlighter-rouge">setcon</code> para que el siguiente <code class="language-plaintext highlighter-rouge">execve</code> (si lo hubiera) o las syscalls del proceso pasen por el dominio <code class="language-plaintext highlighter-rouge">untrusted_app</code>, <code class="language-plaintext highlighter-rouge">priv_app</code>, <code class="language-plaintext highlighter-rouge">platform_app</code>, etc., según el <code class="language-plaintext highlighter-rouge">seinfo</code> que asigna <code class="language-plaintext highlighter-rouge">PackageManager</code>.</li> <li><code class="language-plaintext highlighter-rouge">SetUpSeccompFilter</code> instala un BPF filter distinto según se trate de <code class="language-plaintext highlighter-rouge">system_server</code>, app o app aislada (<code class="language-plaintext highlighter-rouge">isolated_app</code>). El filtro restringe el conjunto de syscalls; cualquier desviación produce <code class="language-plaintext highlighter-rouge">SIGSYS</code>.</li> </ul> <h3 id="52-preapplicationinit-y-el-regreso-a-java">5.2 <code class="language-plaintext highlighter-rouge">PreApplicationInit</code> y el regreso a Java</h3> <p>Al final, en el hijo se llaman los hooks de Java <code class="language-plaintext highlighter-rouge">Zygote.callPostForkChildHooks</code>, que:</p> <ul> <li>Resetean el <code class="language-plaintext highlighter-rouge">Random</code> per-process (no queremos que dos apps deriven la misma secuencia).</li> <li>Notifican a ART que ahora puede activar JIT/profile saving (en el Zygote estaba deshabilitado).</li> <li>Reactivan el <em>heap task daemon</em>.</li> <li>Resetean caches dependientes de PID/UID.</li> </ul> <p>Y por fin se devuelve un <code class="language-plaintext highlighter-rouge">Runnable</code> al <code class="language-plaintext highlighter-rouge">runSelectLoop</code>. Ese <code class="language-plaintext highlighter-rouge">Runnable</code> es lo que ejecutará el <code class="language-plaintext highlighter-rouge">main</code> de la app.</p> <h2 id="6-de-zygote-a-activitythreadmain">6. De <code class="language-plaintext highlighter-rouge">Zygote</code> a <code class="language-plaintext highlighter-rouge">ActivityThread.main</code></h2> <p>El <code class="language-plaintext highlighter-rouge">Runnable</code> devuelto por el Zygote, para una app normal, lo construye <code class="language-plaintext highlighter-rouge">ZygoteConnection.handleChildProc -&gt; ZygoteInit.zygoteInit</code>:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">static</span> <span class="kd">final</span> <span class="nc">Runnable</span> <span class="nf">zygoteInit</span><span class="o">(</span><span class="kt">int</span> <span class="n">targetSdkVersion</span><span class="o">,</span> <span class="kt">long</span><span class="o">[]</span> <span class="n">disabledCompatChanges</span><span class="o">,</span>
        <span class="nc">String</span><span class="o">[]</span> <span class="n">argv</span><span class="o">,</span> <span class="nc">ClassLoader</span> <span class="n">classLoader</span><span class="o">)</span> <span class="o">{</span>
    <span class="nc">RuntimeInit</span><span class="o">.</span><span class="na">redirectLogStreams</span><span class="o">();</span>

    <span class="nc">RuntimeInit</span><span class="o">.</span><span class="na">commonInit</span><span class="o">();</span>
    <span class="nc">ZygoteInit</span><span class="o">.</span><span class="na">nativeZygoteInit</span><span class="o">();</span>   <span class="c1">// (1)</span>

    <span class="k">return</span> <span class="nc">RuntimeInit</span><span class="o">.</span><span class="na">applicationInit</span><span class="o">(</span><span class="n">targetSdkVersion</span><span class="o">,</span> <span class="n">disabledCompatChanges</span><span class="o">,</span>
            <span class="n">argv</span><span class="o">,</span> <span class="n">classLoader</span><span class="o">);</span>
<span class="o">}</span>
</code></pre></div></div> <ol> <li><strong><code class="language-plaintext highlighter-rouge">nativeZygoteInit</code></strong> llama, vía JNI, a <code class="language-plaintext highlighter-rouge">AppRuntime::onZygoteInit()</code> que arranca el <strong>Binder thread pool</strong> (<code class="language-plaintext highlighter-rouge">ProcessState::self()-&gt;startThreadPool()</code>). Desde este momento, el proceso puede recibir transacciones Binder.</li> <li><strong><code class="language-plaintext highlighter-rouge">applicationInit</code></strong> parsea los argumentos restantes y reflexivamente invoca el <code class="language-plaintext highlighter-rouge">main</code> de la clase indicada, que para una app es <code class="language-plaintext highlighter-rouge">android.app.ActivityThread</code>:</li> </ol> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">protected</span> <span class="kd">static</span> <span class="nc">Runnable</span> <span class="nf">applicationInit</span><span class="o">(</span><span class="kt">int</span> <span class="n">targetSdkVersion</span><span class="o">,</span> <span class="kt">long</span><span class="o">[]</span> <span class="n">disabledCompatChanges</span><span class="o">,</span>
        <span class="nc">String</span><span class="o">[]</span> <span class="n">argv</span><span class="o">,</span> <span class="nc">ClassLoader</span> <span class="n">classLoader</span><span class="o">)</span> <span class="o">{</span>
    <span class="n">nativeSetExitWithoutCleanup</span><span class="o">(</span><span class="kc">true</span><span class="o">);</span>
    <span class="nc">VMRuntime</span><span class="o">.</span><span class="na">getRuntime</span><span class="o">().</span><span class="na">setTargetSdkVersion</span><span class="o">(</span><span class="n">targetSdkVersion</span><span class="o">);</span>
    <span class="nc">VMRuntime</span><span class="o">.</span><span class="na">getRuntime</span><span class="o">().</span><span class="na">setDisabledCompatChanges</span><span class="o">(</span><span class="n">disabledCompatChanges</span><span class="o">);</span>

    <span class="kd">final</span> <span class="nc">Arguments</span> <span class="n">args</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Arguments</span><span class="o">(</span><span class="n">argv</span><span class="o">);</span>

    <span class="k">return</span> <span class="nf">findStaticMain</span><span class="o">(</span><span class="n">args</span><span class="o">.</span><span class="na">startClass</span><span class="o">,</span> <span class="n">args</span><span class="o">.</span><span class="na">startArgs</span><span class="o">,</span> <span class="n">classLoader</span><span class="o">);</span>
<span class="o">}</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">findStaticMain</code> no invoca directamente, devuelve un <code class="language-plaintext highlighter-rouge">Runnable</code> que cuando se ejecute hará <code class="language-plaintext highlighter-rouge">methodMain.invoke(null, (Object) args)</code>. Eso permite al Zygote <strong>devolver el control limpio</strong> al loop principal: el <code class="language-plaintext highlighter-rouge">Runnable</code> se ejecuta fuera de cualquier <code class="language-plaintext highlighter-rouge">try/catch</code> de Zygote, en el stack mínimo del proceso recién especializado.</p> <h3 id="61-activitythreadmain">6.1 <code class="language-plaintext highlighter-rouge">ActivityThread.main</code></h3> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">static</span> <span class="kt">void</span> <span class="nf">main</span><span class="o">(</span><span class="nc">String</span><span class="o">[]</span> <span class="n">args</span><span class="o">)</span> <span class="o">{</span>
    <span class="nc">Trace</span><span class="o">.</span><span class="na">traceBegin</span><span class="o">(</span><span class="nc">Trace</span><span class="o">.</span><span class="na">TRACE_TAG_ACTIVITY_MANAGER</span><span class="o">,</span> <span class="s">"ActivityThreadMain"</span><span class="o">);</span>

    <span class="nc">Looper</span><span class="o">.</span><span class="na">prepareMainLooper</span><span class="o">();</span>

    <span class="nc">ActivityThread</span> <span class="n">thread</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">ActivityThread</span><span class="o">();</span>
    <span class="n">thread</span><span class="o">.</span><span class="na">attach</span><span class="o">(</span><span class="kc">false</span><span class="o">,</span> <span class="n">startSeq</span><span class="o">);</span>   <span class="c1">// &lt;-- llama a AMS.attachApplication</span>

    <span class="k">if</span> <span class="o">(</span><span class="n">sMainThreadHandler</span> <span class="o">==</span> <span class="kc">null</span><span class="o">)</span> <span class="o">{</span>
        <span class="n">sMainThreadHandler</span> <span class="o">=</span> <span class="n">thread</span><span class="o">.</span><span class="na">getHandler</span><span class="o">();</span>
    <span class="o">}</span>

    <span class="nc">Looper</span><span class="o">.</span><span class="na">loop</span><span class="o">();</span>
    <span class="k">throw</span> <span class="k">new</span> <span class="nf">RuntimeException</span><span class="o">(</span><span class="s">"Main thread loop unexpectedly exited"</span><span class="o">);</span>
<span class="o">}</span>
</code></pre></div></div> <p>Cosas importantes:</p> <ul> <li><strong><code class="language-plaintext highlighter-rouge">Looper.prepareMainLooper()</code></strong>: instala el <code class="language-plaintext highlighter-rouge">MessageQueue</code> del hilo principal. El <em>UI thread</em> y el <em>main thread</em> son el mismo en Android para las apps.</li> <li><strong><code class="language-plaintext highlighter-rouge">thread.attach(false, startSeq)</code></strong> realiza una llamada Binder a <code class="language-plaintext highlighter-rouge">ActivityManagerService.attachApplication(IApplicationThread, long)</code>. Es ese <code class="language-plaintext highlighter-rouge">attachApplication</code> el que cierra el lazo con <code class="language-plaintext highlighter-rouge">system_server</code>: AMS estaba esperando este momento desde que pidió el fork al Zygote. Ahora AMS sabe a qué proceso enviar <code class="language-plaintext highlighter-rouge">bindApplication</code>, <code class="language-plaintext highlighter-rouge">scheduleLaunchActivity</code>, etc.</li> <li>A partir de <code class="language-plaintext highlighter-rouge">Looper.loop()</code>, el proceso vive despachando mensajes hasta que el sistema lo mate.</li> </ul> <h2 id="7-la-vista-panorámica-secuencia-completa">7. La vista panorámica: secuencia completa</h2> <p>Si juntamos todo, una llamada a <code class="language-plaintext highlighter-rouge">Context.startActivity(intent)</code> que arranca una app fría se traduce, simplificadamente, en:</p> <ol> <li>La app emisora hace IPC Binder a <code class="language-plaintext highlighter-rouge">ActivityManagerService</code> (en <code class="language-plaintext highlighter-rouge">system_server</code>).</li> <li>AMS resuelve el <code class="language-plaintext highlighter-rouge">ComponentName</code>, comprueba permisos, encuentra que el proceso <em>target</em> no existe.</li> <li>AMS llama a <code class="language-plaintext highlighter-rouge">Process.start("android.app.ActivityThread", ...)</code> que termina hablando por el socket Unix <code class="language-plaintext highlighter-rouge">/dev/socket/zygote</code> con un comando textual.</li> <li><code class="language-plaintext highlighter-rouge">ZygoteServer.runSelectLoop</code> despierta del <code class="language-plaintext highlighter-rouge">poll</code>, lee el comando, construye <code class="language-plaintext highlighter-rouge">ZygoteArguments</code> y llama a <code class="language-plaintext highlighter-rouge">Zygote.forkAndSpecialize</code>.</li> <li>JNI: <code class="language-plaintext highlighter-rouge">ForkCommon</code> cierra <code class="language-plaintext highlighter-rouge">fd</code> no allowlisted y hace <code class="language-plaintext highlighter-rouge">fork()</code>.</li> <li>El kernel ejecuta <code class="language-plaintext highlighter-rouge">copy_process</code> (COW del heap ART, duplicado de fd allowed, herencia de namespaces, capabilities, seccomp, contexto SELinux).</li> <li>En el hijo, <code class="language-plaintext highlighter-rouge">SpecializeCommon</code> aplica UID/GID, GIDs suplementarios, drop de capabilities, transición SELinux y seccomp final.</li> <li><code class="language-plaintext highlighter-rouge">nativeZygoteInit</code> arranca el pool de hilos Binder.</li> <li><code class="language-plaintext highlighter-rouge">RuntimeInit.applicationInit</code> devuelve el <code class="language-plaintext highlighter-rouge">Runnable</code> que llama a <code class="language-plaintext highlighter-rouge">ActivityThread.main</code>.</li> <li><code class="language-plaintext highlighter-rouge">ActivityThread.attach</code> llama a <code class="language-plaintext highlighter-rouge">AMS.attachApplication</code> por Binder.</li> <li>AMS responde con <code class="language-plaintext highlighter-rouge">bindApplication</code> + <code class="language-plaintext highlighter-rouge">scheduleLaunchActivity</code>, y la actividad se infla.</li> </ol> <p>Visualmente:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>init (PID 1)
  └── app_process64 --zygote --start-system-server   (Zygote, UID 0)
        ├── system_server (UID 1000)
        │     └── ActivityManagerService
        ├── com.android.systemui (UID 10078)
        ├── com.google.android.gms (UID 10095)
        └── com.example.app (UID 10212)              &lt;-- nuestro fork
</code></pre></div></div> <h2 id="8-implicaciones-de-seguridad">8. Implicaciones de seguridad</h2> <p>Esta arquitectura tiene consecuencias de seguridad muy concretas que conviene tener presentes:</p> <h3 id="81-el-socket-del-zygote-es-un-punto-de-poder-enorme">8.1 El socket del Zygote es un punto de poder enorme</h3> <p>Quien pueda escribir comandos válidos en <code class="language-plaintext highlighter-rouge">/dev/socket/zygote</code> puede pedir un fork con cualquier UID y <code class="language-plaintext highlighter-rouge">seinfo</code> que la peer-credential validation permita. Por eso:</p> <ul> <li>El socket es <code class="language-plaintext highlighter-rouge">0660 root:system</code>. Solo procesos del grupo <code class="language-plaintext highlighter-rouge">system</code> (esencialmente <code class="language-plaintext highlighter-rouge">system_server</code>) escriben.</li> <li>El Zygote valida <code class="language-plaintext highlighter-rouge">SO_PEERCRED</code> y rechaza peticiones de UIDs no esperados.</li> <li>Hubo CVEs históricos (por ejemplo, alrededor del parseo de argumentos o de fugas de <code class="language-plaintext highlighter-rouge">fd</code>) precisamente en esta superficie.</li> </ul> <h3 id="82-herencia-descontrolada-de-descriptores--escalada">8.2 Herencia descontrolada de descriptores = escalada</h3> <p>Si Zygote dejara abierto, accidentalmente, un <code class="language-plaintext highlighter-rouge">fd</code> a, digamos, <code class="language-plaintext highlighter-rouge">/data/system/packages.xml</code>, <strong>todas las apps</strong> lo heredarían. Por eso existe <code class="language-plaintext highlighter-rouge">FileDescriptorAllowlist</code>: se mantiene una whitelist explícita y todo lo demás aborta el proceso. Cualquier ingeniero que añada un <code class="language-plaintext highlighter-rouge">open()</code> en el path del Zygote debe registrar el path en la allowlist o asegurarse de cerrarlo antes del fork.</p> <h3 id="83-capabilities-y-pr_set_no_new_privs">8.3 Capabilities y <code class="language-plaintext highlighter-rouge">PR_SET_NO_NEW_PRIVS</code></h3> <p>Las apps acaban con conjunto de capabilities <strong>vacío</strong> y <code class="language-plaintext highlighter-rouge">NO_NEW_PRIVS=1</code>, lo que invalida bits SUID/SGID en <code class="language-plaintext highlighter-rouge">execve</code>. Esto es lo que hace inviable elevar privilegios desde una app simplemente ejecutando un binario.</p> <h3 id="84-selinux-divide-la-superficie">8.4 SELinux divide la superficie</h3> <p>Tras <code class="language-plaintext highlighter-rouge">selinux_android_setcontext</code>, una app cae típicamente en <code class="language-plaintext highlighter-rouge">untrusted_app_&lt;sdk&gt;</code>. La política prohíbe explícitamente:</p> <ul> <li>Abrir la mayoría de devices de <code class="language-plaintext highlighter-rouge">/dev</code> salvo los necesarios (binder, ashmem, render node).</li> <li>Conectarse a sockets de servicios privilegiados.</li> <li>Leer rutas como <code class="language-plaintext highlighter-rouge">/proc/&lt;pid&gt;</code> de otros UIDs.</li> </ul> <p>Esto se aplica <strong>después</strong> del fork, lo cual significa que <strong>cualquier acción que el Zygote haga antes de la especialización corre con el dominio del Zygote (<code class="language-plaintext highlighter-rouge">zygote</code>)</strong>, no con el dominio de la app. Cuidado al añadir hooks en <code class="language-plaintext highlighter-rouge">preload()</code>.</p> <h3 id="85-seccomp-como-red-de-seguridad">8.5 Seccomp como red de seguridad</h3> <p>El filtro seccomp instalado en apps bloquea syscalls “raras” (e.g. <code class="language-plaintext highlighter-rouge">add_key</code>, <code class="language-plaintext highlighter-rouge">keyctl</code>, varias de <code class="language-plaintext highlighter-rouge">ioperm</code>, <code class="language-plaintext highlighter-rouge">ptrace</code> con condiciones, etc.) que rara vez necesita código Java pero que han sido históricamente vector de exploit a kernel. Un <code class="language-plaintext highlighter-rouge">SIGSYS</code> mata a la app inmediatamente y aparece en el dropbox.</p> <h3 id="86-análisis-forense">8.6 Análisis forense</h3> <p>Para detectar uso anómalo de <code class="language-plaintext highlighter-rouge">app_process</code> (por ejemplo, malware que se relanza con un <code class="language-plaintext highlighter-rouge">nice_name</code> legítimo pero clase de entrada distinta), las señales útiles son:</p> <ul> <li><code class="language-plaintext highlighter-rouge">/proc/&lt;pid&gt;/cmdline</code> muestra los argumentos originales: si ves <code class="language-plaintext highlighter-rouge">app_process</code> con <code class="language-plaintext highlighter-rouge">--application</code> y una clase no estándar, sospecha.</li> <li><code class="language-plaintext highlighter-rouge">/proc/&lt;pid&gt;/status</code> te da <code class="language-plaintext highlighter-rouge">PPid</code>: un proceso de app cuyo <code class="language-plaintext highlighter-rouge">PPid</code> <strong>no es</strong> el del Zygote es muy raro.</li> <li><code class="language-plaintext highlighter-rouge">/proc/&lt;pid&gt;/attr/current</code> te dice el dominio SELinux. Una “app” con dominio <code class="language-plaintext highlighter-rouge">shell</code> o <code class="language-plaintext highlighter-rouge">su</code> es señal clarísima de inyección manual.</li> </ul> <h2 id="9-cosas-que-la-gente-suele-entender-mal">9. Cosas que la gente suele entender mal</h2> <ul> <li><strong>“Zygote usa <code class="language-plaintext highlighter-rouge">exec</code>“</strong>: no. Zygote <strong>nunca</strong> hace <code class="language-plaintext highlighter-rouge">execve</code> para crear apps. Solo <code class="language-plaintext highlighter-rouge">fork</code>. Por eso el COW funciona y por eso las clases preloaded sirven de algo.</li> <li><strong>“Cada app tiene su propia VM”</strong>: a nivel lógico sí, pero comparten la imagen <code class="language-plaintext highlighter-rouge">boot.art</code> mapeada y muchas páginas de heap inicial por COW. La VM “instance” es lógica.</li> <li><strong>“El UID se asigna al instalar”</strong>: sí, <code class="language-plaintext highlighter-rouge">PackageManager</code> asigna un UID al instalar la app, pero el proceso solo adquiere ese UID en el <code class="language-plaintext highlighter-rouge">setresuid</code> post-fork. Antes del fork, el Zygote es root.</li> <li><strong>“<code class="language-plaintext highlighter-rouge">system_server</code> es un proceso aparte del Zygote”</strong>: lo es <em>conceptualmente</em>, pero <strong>es un fork temprano del Zygote</strong>, no un <code class="language-plaintext highlighter-rouge">exec</code> independiente. Por eso muchas clases del framework ya están cargadas en él.</li> <li><strong>“<code class="language-plaintext highlighter-rouge">app_process</code> y <code class="language-plaintext highlighter-rouge">dalvikvm</code> son lo mismo”</strong>: muy parecidos, pero <code class="language-plaintext highlighter-rouge">app_process</code> arranca con la convención del runtime Android (Zygote o no), mientras <code class="language-plaintext highlighter-rouge">dalvikvm</code> (donde aún existe) es un wrapper minimalista para ejecutar <code class="language-plaintext highlighter-rouge">main</code> de un jar.</li> </ul> <h2 id="10-referencias-y-lecturas-recomendadas">10. Referencias y lecturas recomendadas</h2> <p>Para profundizar, las fuentes primarias son insuperables (todo está en AOSP):</p> <ul> <li><code class="language-plaintext highlighter-rouge">frameworks/base/cmds/app_process/app_main.cpp</code></li> <li><code class="language-plaintext highlighter-rouge">frameworks/base/core/jni/AndroidRuntime.cpp</code></li> <li><code class="language-plaintext highlighter-rouge">frameworks/base/core/jni/com_android_internal_os_Zygote.cpp</code></li> <li><code class="language-plaintext highlighter-rouge">frameworks/base/core/java/com/android/internal/os/ZygoteInit.java</code></li> <li><code class="language-plaintext highlighter-rouge">frameworks/base/core/java/com/android/internal/os/Zygote.java</code></li> <li><code class="language-plaintext highlighter-rouge">frameworks/base/core/java/com/android/internal/os/ZygoteServer.java</code></li> <li><code class="language-plaintext highlighter-rouge">frameworks/base/core/java/com/android/internal/os/ZygoteConnection.java</code></li> <li><code class="language-plaintext highlighter-rouge">frameworks/base/core/java/com/android/internal/os/RuntimeInit.java</code></li> <li><code class="language-plaintext highlighter-rouge">frameworks/base/core/java/android/app/ActivityThread.java</code></li> <li><code class="language-plaintext highlighter-rouge">system/core/init/</code> y <code class="language-plaintext highlighter-rouge">system/core/rootdir/init.zygote*.rc</code></li> <li><code class="language-plaintext highlighter-rouge">bionic/libc/bionic/fork.cpp</code></li> <li>En el kernel: <code class="language-plaintext highlighter-rouge">kernel/fork.c</code>, en concreto <code class="language-plaintext highlighter-rouge">copy_process()</code> y <code class="language-plaintext highlighter-rouge">dup_mmap()</code>.</li> </ul> <p>Dos consejos prácticos para explorar AOSP por tu cuenta:</p> <ol> <li>Usa <code class="language-plaintext highlighter-rouge">cs.android.com</code> (Code Search de Google) y enlaza por símbolo: <code class="language-plaintext highlighter-rouge">Zygote.forkAndSpecialize</code>, <code class="language-plaintext highlighter-rouge">ForkCommon</code>, <code class="language-plaintext highlighter-rouge">SpecializeCommon</code>.</li> <li>En un dispositivo real, <code class="language-plaintext highlighter-rouge">dumpsys activity processes</code> y <code class="language-plaintext highlighter-rouge">cat /proc/&lt;zygote_pid&gt;/maps | head</code> enseñan cuánta memoria es compartida vía COW. Es muy ilustrativo.</li> </ol> <hr/> <p>Con esto debería quedar clara la trazabilidad completa: un <code class="language-plaintext highlighter-rouge">startActivity</code> de tu app cruza Binder, IPCs, sockets Unix, una syscall <code class="language-plaintext highlighter-rouge">clone</code>, miles de líneas de inicialización Java y termina en un <code class="language-plaintext highlighter-rouge">Looper.loop()</code> que parece magia… pero es la cuidadosa coreografía que acabamos de recorrer.</p>]]></content><author><name></name></author><category term="android-internals"/><category term="android"/><category term="internals"/><category term="zygote"/><category term="kernel"/><category term="security"/><summary type="html"><![CDATA[Walkthrough técnico y detallado del ciclo completo de creación de un proceso en Android, desde fork() en el kernel Linux hasta el bootstrap de una aplicación a través de Zygote y app_process.]]></summary></entry><entry><title type="html">Introducción a la explotación moderna de V8: laboratorio, heap cage, trusted space y sandbox</title><link href="https://secoperator.github.io/blog/2026/modern-v8-exploitation-intro/" rel="alternate" type="text/html" title="Introducción a la explotación moderna de V8: laboratorio, heap cage, trusted space y sandbox"/><published>2026-05-18T10:00:00+00:00</published><updated>2026-05-18T10:00:00+00:00</updated><id>https://secoperator.github.io/blog/2026/modern-v8-exploitation-intro</id><content type="html" xml:base="https://secoperator.github.io/blog/2026/modern-v8-exploitation-intro/"><![CDATA[<p>V8 ha cambiado más en los últimos cinco años que en toda la década anterior. La introducción de <strong>pointer compression</strong>, la separación de objetos en distintos <strong>spaces</strong> según su nivel de confianza, y finalmente la aparición del <strong>V8 sandbox</strong> han reescrito por completo el camino que recorre un atacante desde un <em>type confusion</em> en TurboFan hasta <code class="language-plaintext highlighter-rouge">RCE</code> en el proceso del renderer.</p> <p>Este post inaugura la serie. Es una <strong>guía de orientación</strong>: vamos a montar el laboratorio mínimo, fijar el vocabulario (cage, trusted space, sandbox), y dibujar el mapa de memoria sobre el que vamos a trabajar en los siguientes posts. No vamos a desarrollar un exploit aquí — vamos a explicar <em>en qué tablero</em> se juega hoy.</p> <blockquote class="block-tip"> <h5 id="tip">TIP</h5> <p>Todo lo que sigue está orientado a investigación defensiva, CTFs y análisis de CVEs públicos. Las flags de <code class="language-plaintext highlighter-rouge">d8</code> que vamos a usar (<code class="language-plaintext highlighter-rouge">--allow-natives-syntax</code>, <code class="language-plaintext highlighter-rouge">--sandbox-testing</code>, etc.) están deshabilitadas en Chrome de producción precisamente porque rompen las invariantes de seguridad del motor.</p> </blockquote> <hr/> <h2 id="1-por-qué-v8-hoy-es-un-objetivo-distinto">1. Por qué V8 hoy es un objetivo distinto</h2> <p>Tres cambios estructurales explican por qué los exploits de V8 que funcionaban en 2018 no son trasladables sin cirugía mayor:</p> <ol> <li><strong>Pointer compression (≈ 2020)</strong> — los punteros a heap dejaron de ser de 64 bits. Ahora son <strong>offsets de 32 bits</strong> dentro de una región reservada, la <em>heap cage</em> (también llamada <em>pointer compression cage</em>).</li> <li><strong>Trusted space y trusted pointer table (≈ 2023)</strong> — objetos cuyos campos son <strong>interpretados como código o como tablas de salto</strong> (<code class="language-plaintext highlighter-rouge">Code</code>, <code class="language-plaintext highlighter-rouge">BytecodeArray</code>, <code class="language-plaintext highlighter-rouge">WasmTrustedInstanceData</code>, …) viven fuera de la cage en una región separada que el atacante <em>no</em> puede alcanzar mediante corrupción dentro de la heap principal.</li> <li><strong>V8 sandbox (estable en 2024+)</strong> — el modelo formaliza que <strong>una corrupción arbitraria dentro de la cage no debe permitir escapar a <code class="language-plaintext highlighter-rouge">RCE</code></strong>. Los punteros sensibles se indirectan a través de tablas externas. Romper esto se considera bug de seguridad por sí mismo.</li> </ol> <p>Visto en una línea de tiempo:</p> <pre><code class="language-mermaid">timeline
    title Evolución del modelo de memoria de V8
    2018 : Punteros 64-bit en toda la heap
         : addrof/fakeobj clásicos
    2020 : Pointer compression
         : Heap cage de 4 GB
         : Compressed pointers (32-bit)
    2023 : Trusted space + indirect pointer tables
         : Code, BytecodeArray fuera de la cage
    2024 : V8 sandbox estable
         : Threat model formalizado
         : Sandboxed pointers, external pointer table
</code></pre> <p>El <strong>threat model del sandbox</strong> está documentado en el repo upstream — vale la pena leerlo entero antes de empezar:</p> <ul> <li><a href="https://chromium.googlesource.com/v8/v8/+/refs/heads/main/src/sandbox/README.md"><code class="language-plaintext highlighter-rouge">src/sandbox/README.md</code></a></li> <li><a href="https://docs.google.com/document/d/1FM4fQmIhEqPG8uGp5o9A-mnPB5BOeScZYpkHjo0KKA8/">V8 sandbox design doc</a></li> </ul> <hr/> <h2 id="2-montando-el-laboratorio-d8-desde-fuente">2. Montando el laboratorio: <code class="language-plaintext highlighter-rouge">d8</code> desde fuente</h2> <p><code class="language-plaintext highlighter-rouge">d8</code> es el <em>developer shell</em> de V8 — un binario standalone que embebe la máquina virtual sin todo el peso de Chrome. Es el lugar donde se desarrollan PoCs y se reproducen CVEs históricos antes de portarlos al renderer.</p> <h3 id="21-obtener-depot_tools-y-el-código">2.1 Obtener <code class="language-plaintext highlighter-rouge">depot_tools</code> y el código</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Pre-requisitos en Debian/Ubuntu</span>
<span class="nb">sudo </span>apt <span class="nb">install</span> <span class="nt">-y</span> git curl python3 pkg-config build-essential

<span class="c"># depot_tools (gclient, gn, ninja, fetch)</span>
git clone https://chromium.googlesource.com/chromium/tools/depot_tools.git
<span class="nb">export </span><span class="nv">PATH</span><span class="o">=</span><span class="s2">"</span><span class="nv">$PWD</span><span class="s2">/depot_tools:</span><span class="nv">$PATH</span><span class="s2">"</span>

<span class="c"># Sincronizar V8</span>
<span class="nb">mkdir </span>v8 <span class="o">&amp;&amp;</span> <span class="nb">cd </span>v8
fetch v8
<span class="nb">cd </span>v8
gclient <span class="nb">sync</span>
</code></pre></div></div> <h3 id="22-builds-que-vamos-a-usar">2.2 Builds que vamos a usar</h3> <p>Para investigación necesitamos al menos <strong>dos builds</strong>: uno de release con símbolos para reproducir exploits “como en Chrome”, y uno de debug con <code class="language-plaintext highlighter-rouge">DCHECK</code>s activos para entender qué invariantes rompe un PoC.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Release con símbolos (rápido, casi como Chrome)</span>
tools/dev/gm.py x64.release
<span class="c"># ./out/x64.release/d8</span>

<span class="c"># Debug (DCHECKs, asserts, lento — útil para crashes)</span>
tools/dev/gm.py x64.debug
<span class="c"># ./out/x64.debug/d8</span>
</code></pre></div></div> <blockquote class="block-tip"> <h5 id="tip-1">TIP</h5> <p>Para empezar es <strong>mucho</strong> más cómodo usar <code class="language-plaintext highlighter-rouge">gm.py</code> que llamar a <code class="language-plaintext highlighter-rouge">gn</code>/<code class="language-plaintext highlighter-rouge">ninja</code> a mano: gestiona los <code class="language-plaintext highlighter-rouge">args.gn</code> por ti y entiende abreviaturas como <code class="language-plaintext highlighter-rouge">x64.release.check</code> (release + DCHECKs).</p> </blockquote> <p>Si quieres replicar exactamente el binario de un commit antiguo (por ejemplo, para reproducir un CVE):</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git checkout &lt;commit-sha-vulnerable&gt;
gclient <span class="nb">sync</span> <span class="nt">-D</span>                  <span class="c"># ajusta dependencias a ese commit</span>
tools/dev/gm.py x64.release
</code></pre></div></div> <h3 id="23-build-con-sandbox-y-con-ayudas-para-atacantes">2.3 Build con sandbox y con ayudas para atacantes</h3> <p>V8 expone un modo especial — el <strong><em>sandbox testing mode</em></strong> — pensado para que un investigador <em>asuma</em> una corrupción arbitraria dentro de la cage y vea qué se puede hacer desde ahí. Se activa con <code class="language-plaintext highlighter-rouge">v8_enable_sandbox = true</code> (ya es el default) y al ejecutar con <code class="language-plaintext highlighter-rouge">--sandbox-testing</code>.</p> <p><code class="language-plaintext highlighter-rouge">args.gn</code> recomendado para el laboratorio:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># out/x64.research/args.gn
</span><span class="n">is_debug</span>              <span class="o">=</span> <span class="n">false</span>
<span class="n">symbol_level</span>          <span class="o">=</span> <span class="mi">2</span>          <span class="c1"># frames legibles en gdb
</span><span class="n">v8_symbol_level</span>       <span class="o">=</span> <span class="mi">2</span>
<span class="n">v8_enable_sandbox</span>     <span class="o">=</span> <span class="n">true</span>       <span class="c1"># V8 sandbox (default, pero explícito)
</span><span class="n">v8_enable_pointer_compression</span> <span class="o">=</span> <span class="n">true</span>
<span class="n">v8_enable_disassembler</span>        <span class="o">=</span> <span class="n">true</span>
<span class="n">v8_enable_object_print</span>        <span class="o">=</span> <span class="n">true</span>   <span class="c1"># %DebugPrint() bonito
</span><span class="n">v8_enable_verify_heap</span>         <span class="o">=</span> <span class="n">true</span>
<span class="n">v8_optimized_debug</span>            <span class="o">=</span> <span class="n">false</span>
<span class="n">dcheck_always_on</span>              <span class="o">=</span> <span class="n">true</span>
</code></pre></div></div> <p>Compila con:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>gn gen out/x64.research
autoninja <span class="nt">-C</span> out/x64.research d8
</code></pre></div></div> <hr/> <h2 id="3-flags-de-d8-que-vas-a-usar-todo-el-rato">3. Flags de <code class="language-plaintext highlighter-rouge">d8</code> que vas a usar todo el rato</h2> <p>Estas son las flags que aparecen en el 90% de los PoCs que vas a leer:</p> <table> <thead> <tr> <th>Flag</th> <th>Para qué sirve</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">--allow-natives-syntax</code></td> <td>Habilita <code class="language-plaintext highlighter-rouge">%FunctionName(...)</code> desde JS: <code class="language-plaintext highlighter-rouge">%DebugPrint</code>, <code class="language-plaintext highlighter-rouge">%SystemBreak</code>, etc.</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">--sandbox-testing</code></td> <td>Tras inicializar, deja una shell con primitivas que <strong>asumen</strong> corrupción.</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">--print-bytecode</code></td> <td>Volcado del bytecode de Ignition para cada función.</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">--trace-turbo</code> / <code class="language-plaintext highlighter-rouge">--trace-turbo-graph</code></td> <td>Logs de TurboFan + JSON para <code class="language-plaintext highlighter-rouge">turbolizer</code>.</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">--trace-opt</code> / <code class="language-plaintext highlighter-rouge">--trace-deopt</code></td> <td>Cuándo una función se optimiza o se <em>deoptimiza</em> y por qué.</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">--print-opt-code</code></td> <td>Imprime el código nativo generado por TurboFan/Maglev.</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">--maglev</code> / <code class="language-plaintext highlighter-rouge">--no-maglev</code></td> <td>Activa/desactiva el JIT intermedio Maglev (entre Ignition y TurboFan).</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">--no-lazy-feedback-allocation</code></td> <td>Fuerza vectores de feedback eagerly — útil para reproducir bugs de specul.</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">--predictable</code></td> <td>Single-thread + GC determinista. Imprescindible para reproducir.</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">--shell</code></td> <td>REPL interactivo.</td> </tr> </tbody> </table> <p>Ejemplo de sesión típica para inspeccionar un objeto:</p> <div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// d8 --allow-natives-syntax</span>
<span class="kd">let</span> <span class="nx">a</span> <span class="o">=</span> <span class="p">[</span><span class="mf">1.1</span><span class="p">,</span> <span class="mf">2.2</span><span class="p">,</span> <span class="mf">3.3</span><span class="p">];</span>
<span class="o">%</span><span class="nc">DebugPrint</span><span class="p">(</span><span class="nx">a</span><span class="p">);</span>
<span class="o">%</span><span class="nc">SystemBreak</span><span class="p">();</span>   <span class="c1">// SIGTRAP — gdb se engancha aquí</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">%DebugPrint</code> te da algo como (resumido):</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>DebugPrint: 0x1f8c0009a4e9: [JSArray]
 - map: 0x1f8c0014a3e9 &lt;Map(PACKED_DOUBLE_ELEMENTS)&gt;
 - prototype: 0x1f8c0014a169 &lt;JSArray[0]&gt;
 - elements: 0x1f8c0009a4d1 &lt;FixedDoubleArray[3]&gt;
 - length: 3
 - properties: 0x1f8c000006e1 &lt;FixedArray[0]&gt;
</code></pre></div></div> <p>Esas direcciones que ves (<code class="language-plaintext highlighter-rouge">0x1f8c0009a4e9</code>) son <strong>direcciones completas de 64 bits</strong> que <code class="language-plaintext highlighter-rouge">%DebugPrint</code> reconstruye sumando la base de la cage al puntero comprimido de 32 bits que V8 realmente guarda en el objeto. Volvemos a esto en la sección 5.</p> <h3 id="31-logs-útiles-más-allá-de-d8">3.1 Logs útiles más allá de <code class="language-plaintext highlighter-rouge">d8</code></h3> <ul> <li><code class="language-plaintext highlighter-rouge">--logfile=- --log-all</code> → todo el <code class="language-plaintext highlighter-rouge">v8.log</code> por stdout (útil para <code class="language-plaintext highlighter-rouge">tick-processor</code>).</li> <li><code class="language-plaintext highlighter-rouge">--prof</code> + <code class="language-plaintext highlighter-rouge">tools/linux-tick-processor</code> → perfil de CPU.</li> <li><code class="language-plaintext highlighter-rouge">--trace-gc</code> / <code class="language-plaintext highlighter-rouge">--trace-gc-verbose</code> → cada GC mayor/menor con tamaños por space.</li> <li><code class="language-plaintext highlighter-rouge">--trace-maps</code> → cada vez que se crea o se promociona un <code class="language-plaintext highlighter-rouge">Map</code> (transiciones de shape).</li> </ul> <p>Para auditoría de optimizaciones específicamente:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>d8 <span class="nt">--allow-natives-syntax</span> <span class="se">\</span>
   <span class="nt">--trace-turbo</span> <span class="nt">--trace-turbo-graph</span> <span class="se">\</span>
   <span class="nt">--trace-opt</span> <span class="nt">--trace-deopt</span> <span class="se">\</span>
   poc.js 2&gt;&amp;1 | <span class="nb">tee </span>run.log
</code></pre></div></div> <p>El JSON <code class="language-plaintext highlighter-rouge">turbo-*.json</code> que cae en el cwd se abre con <a href="https://v8.github.io/tools/head/turbolizer/index.html">Turbolizer</a> y te muestra los grafos de TurboFan paso a paso — imprescindible para entender bugs de <em>typer</em> o <em>escape analysis</em>.</p> <hr/> <h2 id="4-pointer-compression-la-geografía-de-la-heap">4. Pointer compression: la geografía de la heap</h2> <p>Antes de hablar de cage o trusted space tenemos que entender <strong>qué es un puntero en V8 hoy</strong>. Spoiler: no es lo que aprendimos.</p> <h3 id="41-la-idea">4.1 La idea</h3> <p>En 64 bits, un puntero ocupa 8 bytes. En una VM con millones de objetos pequeños eso es muchísima memoria gastada solo en <em>referencias</em>. <strong>Pointer compression</strong> explota una observación:</p> <blockquote> <p>Si reservamos una región contigua de <strong>4 GB alineada a 4 GB</strong> para todo el heap, podemos representar cualquier puntero dentro de esa región con solo <strong>32 bits</strong> (un offset). Para “descomprimirlo” basta con sumarle la <strong>base de la cage</strong>.</p> </blockquote> <p>A esa región se le llama indistintamente <em>pointer compression cage</em>, <em>main cage</em> o simplemente <em>V8 heap</em>. Vive en <code class="language-plaintext highlighter-rouge">IsolateGroup::GetPtrComprCage()</code>:</p> <ul> <li><a href="https://chromium.googlesource.com/v8/v8/+/refs/heads/main/src/common/ptr-compr-inl.h"><code class="language-plaintext highlighter-rouge">src/common/ptr-compr-inl.h</code></a></li> <li><a href="https://chromium.googlesource.com/v8/v8/+/refs/heads/main/src/heap/heap.h"><code class="language-plaintext highlighter-rouge">src/heap/heap.h</code></a></li> </ul> <pre><code class="language-typograms">0x1f8c00000000
+--------------------------------------------------+ &lt;- cage base (alineada a 4 GB)
| young generation (new_space)                     |
| - allocation hot path, scavenger                 |
+--------------------------------------------------+
| old_space    (objetos promovidos, JSObject...)   |
+--------------------------------------------------+
| code_space   (legacy, cada vez menos usado)      |
+--------------------------------------------------+
| large_object_space                               |
+--------------------------------------------------+
| read_only_space (mapas inmutables, undefined...) |
+--------------------------------------------------+ &lt;- cage base + 4 GB
</code></pre> <p>Dentro de la cage los punteros se guardan como <code class="language-plaintext highlighter-rouge">Tagged_t</code> (32 bits). La operación de descompresión es esencialmente:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// src/common/ptr-compr-inl.h (simplificado)</span>
<span class="n">Address</span> <span class="nf">DecompressTagged</span><span class="p">(</span><span class="n">Tagged_t</span> <span class="n">compressed</span><span class="p">)</span> <span class="p">{</span>
  <span class="k">return</span> <span class="n">cage_base_</span> <span class="o">|</span> <span class="k">static_cast</span><span class="o">&lt;</span><span class="kt">uintptr_t</span><span class="o">&gt;</span><span class="p">(</span><span class="n">compressed</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div> <h3 id="42-smis-y-heapobjects">4.2 Smis y HeapObjects</h3> <p>V8 usa <strong>pointer tagging</strong>. El bit menos significativo distingue:</p> <ul> <li><code class="language-plaintext highlighter-rouge">xxxx...xxx0</code> → <strong>Smi</strong> (Small Integer). El valor cabe en los bits superiores. No es un puntero, no se sigue.</li> <li><code class="language-plaintext highlighter-rouge">xxxx...xxx1</code> → <strong>HeapObject</strong>. El resto es un puntero (en 64-bit clásico) o un offset (con pointer compression).</li> </ul> <pre><code class="language-typograms">HeapObject tagged (32-bit compressed):
+---------------------------------+--+
| offset dentro de la cage   (31) | 1|
+---------------------------------+--+

Smi (32-bit compressed):
+---------------------------------+--+
| valor entero (31 bits, signed)  | 0|
+---------------------------------+--+
</code></pre> <p>Esto tiene una consecuencia explotable famosa: <strong>cualquier <code class="language-plaintext highlighter-rouge">double</code> cuyos 32 bits bajos tengan el bit 0 a 1 puede ser malinterpretado como un puntero comprimido</strong> si conseguimos un <em>type confusion</em> entre <code class="language-plaintext highlighter-rouge">PACKED_DOUBLE_ELEMENTS</code> y <code class="language-plaintext highlighter-rouge">PACKED_ELEMENTS</code>. Es exactamente el sustrato de los <em>addrof/fakeobj</em> modernos en V8.</p> <h3 id="43-consecuencias-para-el-atacante">4.3 Consecuencias para el atacante</h3> <table> <thead> <tr> <th>Antes (sin compresión)</th> <th>Hoy (con compresión)</th> </tr> </thead> <tbody> <tr> <td>OOB write da puntero 64-bit arbitrario</td> <td>OOB write solo te da <strong>32 bits</strong> — estás encerrado en 4 GB</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">addrof</code> produce dirección absoluta</td> <td><code class="language-plaintext highlighter-rouge">addrof</code> produce <strong>offset desde la cage base</strong></td> </tr> <tr> <td>Leer/escribir cualquier dirección del proceso</td> <td>Por defecto: solo dentro de la cage</td> </tr> <tr> <td>Pivotar a libc / <code class="language-plaintext highlighter-rouge">system()</code> es directo</td> <td>Necesitas un <em>out-of-cage primitive</em> (sandbox escape)</td> </tr> </tbody> </table> <p>Esa última fila es precisamente la motivación del V8 sandbox, que veremos en la sección 7.</p> <hr/> <h2 id="5-qué-vive-en-la-heap-cage">5. ¿Qué vive en la heap cage?</h2> <p>La cage es el <strong>terreno donde el atacante asume que ya tiene corrupción arbitraria</strong>. Es importante saber qué pesca uno desde ahí.</p> <p>Conviven en la cage:</p> <ul> <li><strong><code class="language-plaintext highlighter-rouge">JSObject</code>, <code class="language-plaintext highlighter-rouge">JSArray</code>, <code class="language-plaintext highlighter-rouge">JSFunction</code>, <code class="language-plaintext highlighter-rouge">JSTypedArray</code>, <code class="language-plaintext highlighter-rouge">JSArrayBuffer</code></strong> — todos los objetos JS visibles.</li> <li><strong><code class="language-plaintext highlighter-rouge">Map</code></strong> — describe la “shape” de un objeto: tamaño, tipo de elementos, prototipo, transiciones. Es lo que TurboFan especula.</li> <li><strong><code class="language-plaintext highlighter-rouge">FixedArray</code>, <code class="language-plaintext highlighter-rouge">FixedDoubleArray</code></strong> — backing stores de propiedades indexadas.</li> <li><strong><code class="language-plaintext highlighter-rouge">PropertyArray</code>, <code class="language-plaintext highlighter-rouge">NameDictionary</code></strong> — backing de propiedades con nombre.</li> <li><strong><code class="language-plaintext highlighter-rouge">String</code>, <code class="language-plaintext highlighter-rouge">ConsString</code>, <code class="language-plaintext highlighter-rouge">SlicedString</code>, <code class="language-plaintext highlighter-rouge">ExternalString</code></strong> — cadenas.</li> <li><strong><code class="language-plaintext highlighter-rouge">SharedFunctionInfo</code>, <code class="language-plaintext highlighter-rouge">FeedbackVector</code></strong> — metadata por función y por <em>call site</em>.</li> <li><strong><code class="language-plaintext highlighter-rouge">Context</code>, <code class="language-plaintext highlighter-rouge">ScopeInfo</code></strong> — entornos léxicos.</li> </ul> <p>Layout de un <code class="language-plaintext highlighter-rouge">JSArray</code> típico (con <code class="language-plaintext highlighter-rouge">PACKED_DOUBLE_ELEMENTS</code>, tagged 32-bit):</p> <pre><code class="language-typograms">JSArray @ cage_base + 0x9a4e8
+--------+-----------------+----------------------+
| offset | field           | value                |
+--------+-----------------+----------------------+
| +0x00  | map             | -&gt; Map(PACKED_DOUBLE)|
| +0x04  | properties      | -&gt; FixedArray[0]     |
| +0x08  | elements        | -&gt; FixedDoubleArray  |
| +0x0c  | length          | Smi(3)               |
+--------+-----------------+----------------------+

FixedDoubleArray @ ...
+--------+-----------------+----------------------+
| +0x00  | map             | -&gt; Map(FIXED_DOUBLE) |
| +0x04  | length          | Smi(3)               |
| +0x08  | element[0]      | 1.1 (8 bytes raw)    |
| +0x10  | element[1]      | 2.2                  |
| +0x18  | element[2]      | 3.3                  |
+--------+-----------------+----------------------+
</code></pre> <p>Fíjate en una cosa importante: el <strong><code class="language-plaintext highlighter-rouge">map</code></strong> (el shape) también está en la cage. Si tienes write-what-where dentro de la cage, <strong>puedes reescribir el <code class="language-plaintext highlighter-rouge">map</code> de un objeto</strong> y convertir un <code class="language-plaintext highlighter-rouge">JSArray</code> en otra cosa. Históricamente, este truco daba R/W universal en el proceso entero. Hoy te da R/W <strong>dentro de la cage</strong>, y para escapar de ahí hace falta más trabajo (ver sección 7).</p> <h3 id="51-cómo-verificarlo-en-vivo">5.1 Cómo verificarlo en vivo</h3> <div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// d8 --allow-natives-syntax</span>
<span class="kd">let</span> <span class="nx">a</span> <span class="o">=</span> <span class="p">[</span><span class="mf">1.1</span><span class="p">,</span> <span class="mf">2.2</span><span class="p">,</span> <span class="mf">3.3</span><span class="p">];</span>
<span class="kd">let</span> <span class="nx">o</span> <span class="o">=</span> <span class="p">{</span> <span class="na">x</span><span class="p">:</span> <span class="mi">1</span><span class="p">,</span> <span class="na">y</span><span class="p">:</span> <span class="mi">2</span> <span class="p">};</span>

<span class="o">%</span><span class="nc">DebugPrintPtr</span><span class="p">(</span><span class="nx">a</span><span class="p">);</span>   <span class="c1">// muestra el puntero crudo (32-bit comprimido)</span>
<span class="o">%</span><span class="nc">DebugPrintPtr</span><span class="p">(</span><span class="nx">o</span><span class="p">);</span>
<span class="o">%</span><span class="nc">HeapObjectVerify</span><span class="p">(</span><span class="nx">a</span><span class="p">);</span>
</code></pre></div></div> <p>Y si quieres ver <em>todo</em> lo que vive en cada space:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>d8 <span class="nt">--allow-natives-syntax</span> poc.js <span class="se">\</span>
   <span class="nt">--trace-gc</span> <span class="nt">--heap-stats</span>
</code></pre></div></div> <hr/> <h2 id="6-trusted-space-lo-que-el-atacante-no-debe-poder-tocar">6. Trusted space: lo que el atacante <strong>no</strong> debe poder tocar</h2> <p>Hasta 2023, objetos como <code class="language-plaintext highlighter-rouge">Code</code> (código nativo JITeado) y <code class="language-plaintext highlighter-rouge">BytecodeArray</code> (bytecode de Ignition) vivían dentro de la cage. Eso significaba que una R/W dentro de la cage permitía:</p> <ul> <li>Reescribir el campo <code class="language-plaintext highlighter-rouge">instruction_start</code> de un <code class="language-plaintext highlighter-rouge">Code</code> y saltar a shellcode.</li> <li>Reescribir el bytecode de una función para hacer que Ignition interpretase instrucciones controladas.</li> </ul> <p>Ambas técnicas estuvieron en exploits públicos durante años (<code class="language-plaintext highlighter-rouge">p0</code> issue tracker, Pwn2Own writeups). La respuesta de V8 fue mover esos objetos a una región separada: el <strong>trusted space</strong>.</p> <h3 id="61-la-idea">6.1 La idea</h3> <p>El trusted space es <strong>otra región</strong> de memoria, distinta de la cage, que aloja objetos cuyo contenido se interpreta como código o como índices de control de flujo. Los objetos en la cage <strong>no pueden apuntar directamente</strong> a objetos en el trusted space — la referencia va a través de una <strong><code class="language-plaintext highlighter-rouge">TrustedPointerTable</code></strong>.</p> <pre><code class="language-mermaid">flowchart LR
    subgraph CAGE["Heap Cage (4 GB, atacante asume r/w aquí)"]
        JSF["JSFunction"]
        SFI["SharedFunctionInfo"]
        JSF -- "shared() compressed ptr" --&gt; SFI
        SFI -- "trusted_handle: u32 índice" --&gt; IDX1
    end

    subgraph TPT["TrustedPointerTable (fuera de la cage)"]
        IDX1["[0x42] -&gt; 0x7f...A100"]
        IDX2["[0x43] -&gt; 0x7f...A200"]
    end

    subgraph TS["Trusted Space (fuera de la cage)"]
        CODE["Code object&lt;br/&gt;instruction_start"]
        BC["BytecodeArray"]
    end

    IDX1 --&gt; CODE
    IDX2 --&gt; BC

    style CAGE fill:#fde2e2,stroke:#c0392b,color:#000
    style TPT fill:#fff4d6,stroke:#a37a00,color:#000
    style TS fill:#d6f5d6,stroke:#2e7d32,color:#000
</code></pre> <p><code class="language-plaintext highlighter-rouge">SharedFunctionInfo::trusted_function_data</code> ya no es un puntero a <code class="language-plaintext highlighter-rouge">Code</code>. Es un <strong>índice de 32 bits</strong> en la <code class="language-plaintext highlighter-rouge">TrustedPointerTable</code>. Cada entrada de la tabla guarda:</p> <ul> <li>Un puntero de 64 bits al objeto en trusted space.</li> <li>Un <strong>tag</strong> que identifica el tipo esperado (<code class="language-plaintext highlighter-rouge">Code</code>, <code class="language-plaintext highlighter-rouge">BytecodeArray</code>, <code class="language-plaintext highlighter-rouge">WasmTrustedInstanceData</code>, …).</li> </ul> <p>Cuando el VM resuelve el handle, <strong>verifica el tag</strong>. Si un atacante con r/w en la cage cambia el índice para que apunte a otro slot, ese slot tiene su propio tag y la operación falla.</p> <h3 id="62-qué-objetos-viven-en-trusted-space">6.2 Qué objetos viven en trusted space</h3> <ul> <li><strong><code class="language-plaintext highlighter-rouge">Code</code></strong> — código nativo generado por TurboFan / Maglev / Sparkplug.</li> <li><strong><code class="language-plaintext highlighter-rouge">BytecodeArray</code></strong> — bytecode de Ignition.</li> <li><strong><code class="language-plaintext highlighter-rouge">InstructionStream</code></strong> — buffer ejecutable subyacente a <code class="language-plaintext highlighter-rouge">Code</code>.</li> <li><strong><code class="language-plaintext highlighter-rouge">WasmTrustedInstanceData</code>, <code class="language-plaintext highlighter-rouge">WasmDispatchTable</code></strong> — estado de instancia y tablas indirectas de WebAssembly. Críticas para call_indirect en wasm.</li> <li><strong><code class="language-plaintext highlighter-rouge">RegExpData</code></strong> — autómatas compilados de RegExp irregexp.</li> </ul> <p>Una buena referencia es el directorio <a href="https://chromium.googlesource.com/v8/v8/+/refs/heads/main/src/sandbox/"><code class="language-plaintext highlighter-rouge">src/sandbox/</code></a> y específicamente:</p> <ul> <li><code class="language-plaintext highlighter-rouge">trusted-pointer-table.h</code></li> <li><code class="language-plaintext highlighter-rouge">code-pointer-table.h</code> (variante específica para <code class="language-plaintext highlighter-rouge">Code</code>)</li> <li><code class="language-plaintext highlighter-rouge">indirect-pointer-tag.h</code> — la lista de tags válidos</li> </ul> <h3 id="63-por-qué-esto-rompe-las-técnicas-clásicas">6.3 Por qué esto rompe las técnicas clásicas</h3> <blockquote class="block-warning"> <h5 id="warning">WARNING</h5> <p>Si lees un writeup de un exploit V8 anterior a 2023 que escribe sobre <code class="language-plaintext highlighter-rouge">instruction_start</code> o sustituye un <code class="language-plaintext highlighter-rouge">BytecodeArray</code> con uno controlado en la heap, <strong>eso ya no funciona en versiones modernas</strong>. El objeto destino vive fuera de la cage y la referencia es indirecta y tagueada.</p> </blockquote> <p>El camino moderno para llegar a <em>code execution</em> desde corrupción en la cage normalmente pasa por <strong>WebAssembly</strong> (cuyo código JITeado tiene reglas distintas) o por <strong>bugs en la propia indirect pointer table / sandbox</strong>, que son ahora un <em>target</em> de primera clase. Volveremos a esto.</p> <hr/> <h2 id="7-el-v8-sandbox">7. El V8 sandbox</h2> <p>El sandbox <strong>no es una jaula de syscalls</strong> (eso lo hace Chrome a nivel de proceso). El V8 sandbox es un <strong>modelo de memoria</strong> que asume:</p> <blockquote> <p>“Un atacante puede leer y escribir arbitrariamente dentro de la heap cage. A pesar de ello, <strong>no debe poder ejecutar código arbitrario</strong> ni leer/escribir fuera de la cage.”</p> </blockquote> <p>Para sostener esa promesa, todos los punteros que <strong>salen</strong> de la cage se indirectan a través de tablas externas:</p> <pre><code class="language-mermaid">flowchart TB
    subgraph IN["Dentro de la Cage (atacante)"]
        OBJ1["JSTypedArray"]
        OBJ2["JSFunction"]
        OBJ3["External object"]
    end

    subgraph TABLES["Tablas fuera de la cage"]
        EPT["ExternalPointerTable&lt;br/&gt;(backing_store, ArrayBuffer data)"]
        TPT["TrustedPointerTable&lt;br/&gt;(Code, Bytecode, Wasm)"]
        CPT["CodePointerTable&lt;br/&gt;(instruction_start)"]
    end

    subgraph OUT["Recursos externos"]
        BS["ArrayBuffer backing store&lt;br/&gt;(mmap fuera de la cage)"]
        CODE["Trusted Code object"]
        ISTART["Executable memory"]
    end

    OBJ1 -- "ept_handle (32-bit, tagged)" --&gt; EPT --&gt; BS
    OBJ2 -- "trusted_handle (32-bit, tagged)" --&gt; TPT --&gt; CODE
    CODE -- "code_handle (32-bit, tagged)" --&gt; CPT --&gt; ISTART

    style IN fill:#fde2e2,stroke:#c0392b,color:#000
    style TABLES fill:#fff4d6,stroke:#a37a00,color:#000
    style OUT fill:#d6f5d6,stroke:#2e7d32,color:#000
</code></pre> <p>Tres tablas, tres “niveles de exterior”:</p> <table> <thead> <tr> <th>Tabla</th> <th>Qué indirecta</th> <th>Defensa principal</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">ExternalPointerTable</code></td> <td>Buffers C++, callbacks, <code class="language-plaintext highlighter-rouge">embedder_data</code></td> <td>Tagging por tipo + entradas reutilizadas</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">TrustedPointerTable</code></td> <td><code class="language-plaintext highlighter-rouge">Code</code>, <code class="language-plaintext highlighter-rouge">Bytecode</code>, instancias wasm</td> <td>Tags estrictos por tipo de objeto</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">CodePointerTable</code></td> <td><code class="language-plaintext highlighter-rouge">Code -&gt; instruction_start</code> (memoria ejecutable)</td> <td>Slot separado para el entry point</td> </tr> </tbody> </table> <blockquote class="block-danger"> <h5 id="danger">DANGER</h5> <p>Una entrada <strong>mal taggeada</strong> o una tabla <strong>con corrupción</strong> rompen todo el modelo. Por eso las propias tablas viven <strong>fuera de la cage</strong> y se mapean con permisos distintos, y por eso <code class="language-plaintext highlighter-rouge">--sandbox-testing</code> existe: para que la gente de V8 (y los investigadores) le den de palos al modelo asumiendo el peor caso.</p> </blockquote> <h3 id="71-el-modo-asumimos-corrupción---sandbox-testing">7.1 El modo “asumimos corrupción”: <code class="language-plaintext highlighter-rouge">--sandbox-testing</code></h3> <p><code class="language-plaintext highlighter-rouge">--sandbox-testing</code> arranca <code class="language-plaintext highlighter-rouge">d8</code> y, después de inicializar todo, deja un REPL con una serie de <strong>primitivas que simulan que ya tienes R/W en la cage</strong>:</p> <div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// d8 --sandbox-testing</span>
<span class="c1">// La función global Sandbox.* expone primitivas de "asumiendo corrupción"</span>
<span class="nx">Sandbox</span><span class="p">.</span><span class="nx">base</span><span class="p">;</span>                     <span class="c1">// dirección base de la cage</span>
<span class="nx">Sandbox</span><span class="p">.</span><span class="nx">byteLength</span><span class="p">;</span>               <span class="c1">// 4 GB</span>
<span class="nx">Sandbox</span><span class="p">.</span><span class="nx">MemoryView</span><span class="p">;</span>               <span class="c1">// Uint8Array de toda la cage</span>
<span class="nx">Sandbox</span><span class="p">.</span><span class="nf">getAddressOf</span><span class="p">(</span><span class="nx">obj</span><span class="p">);</span>        <span class="c1">// offset de obj dentro de la cage</span>
<span class="nx">Sandbox</span><span class="p">.</span><span class="nf">getObjectAt</span><span class="p">(</span><span class="nx">off</span><span class="p">);</span>         <span class="c1">// recupera el objeto en ese offset</span>
</code></pre></div></div> <p>Esto es <strong>exactamente</strong> lo que un atacante tendría tras un bug de TurboFan típico. La pregunta que el equipo de V8 (y nosotros) quiere responder es: <em>desde aquí, ¿puedes escapar de la cage?</em> Cada vez que la respuesta es “sí”, es un bug de seguridad del sandbox por sí mismo, independientemente del bug que te dio la R/W inicial.</p> <p>Esa es una <strong>inversión enorme</strong> del modelo: el listón para un exploit ya no es “consigo r/w arbitrario”, es “consigo r/w arbitrario <em>y además</em> tengo una primitiva fuera de la cage”.</p> <h3 id="72-lecturas-de-referencia">7.2 Lecturas de referencia</h3> <ul> <li><a href="https://chromium.googlesource.com/v8/v8/+/refs/heads/main/src/sandbox/README.md">V8 sandbox README</a> — modelo y motivación.</li> <li><a href="https://g.co/chrome/vrp">Sandbox VRP rules (Chromium)</a> — Google paga por <em>sandbox escape</em> aunque no haya bug en la VM.</li> <li>Charlas: <strong>Off By One Security</strong>, <em>V8 Sandbox: a journey from idea to production</em>, y los writeups del <em>Pwn2Own 2024/2025</em> renderer category.</li> <li>Issues etiquetados <code class="language-plaintext highlighter-rouge">component:Blink&gt;Bindings,Sandbox</code> en <a href="https://bugs.chromium.org/">bugs.chromium.org</a>.</li> </ul> <hr/> <h2 id="8-mapa-mental-para-la-serie">8. Mapa mental para la serie</h2> <p>Con todo lo anterior podemos dibujar el “tablero” sobre el que se mueve un exploit de V8 hoy:</p> <pre><code class="language-mermaid">flowchart LR
    BUG["Bug raíz&lt;br/&gt;(typer, escape analysis,&lt;br/&gt;OOB en builtin, wasm...)"]
    RW["R/W arbitrario dentro&lt;br/&gt;de la heap cage"]
    SBX_ESC["Escape del&lt;br/&gt;V8 sandbox&lt;br/&gt;(out-of-cage primitive)"]
    RCE["Code exec en el&lt;br/&gt;proceso renderer"]
    BROKER["Salto al broker /&lt;br/&gt;kernel (otra historia)"]

    BUG -- "addrof / fakeobj /&lt;br/&gt;map confusion" --&gt; RW
    RW -- "abuso de external pointer&lt;br/&gt;table, bug del sandbox,&lt;br/&gt;wasm primitives" --&gt; SBX_ESC
    SBX_ESC -- "JIT spray, ROP en&lt;br/&gt;código ejecutable,&lt;br/&gt;wasm jit" --&gt; RCE
    RCE -- "Mojo IPC, GPU,&lt;br/&gt;file:// tricks" --&gt; BROKER

    style BUG fill:#dfe7fd,stroke:#3a5fcd,color:#000
    style RW fill:#fde2e2,stroke:#c0392b,color:#000
    style SBX_ESC fill:#fff4d6,stroke:#a37a00,color:#000
    style RCE fill:#d6f5d6,stroke:#2e7d32,color:#000
    style BROKER fill:#e8d6f5,stroke:#6a1b9a,color:#000
</code></pre> <p>Cada flecha es un <strong>post entero</strong>. En los siguientes vamos a:</p> <ol> <li>Tomar un bug público de TurboFan (un <em>typer bug</em> clásico) y montar <code class="language-plaintext highlighter-rouge">addrof</code>/<code class="language-plaintext highlighter-rouge">fakeobj</code> desde cero <strong>en V8 con pointer compression</strong>.</li> <li>Convertir esas primitivas en R/W de toda la cage usando <code class="language-plaintext highlighter-rouge">ArrayBuffer</code> con backing store interno.</li> <li>Estudiar tres técnicas conocidas para salir de la cage: WASM JIT, abuso de <code class="language-plaintext highlighter-rouge">ExternalPointerTable</code>, y bugs del sandbox propiamente.</li> <li>Discutir cómo se integra todo en una cadena renderer → broker.</li> </ol> <hr/> <h2 id="9-referencias-rápidas">9. Referencias rápidas</h2> <p>Código upstream que vale la pena tener abierto en una pestaña:</p> <ul> <li><a href="https://chromium.googlesource.com/v8/v8/+/refs/heads/main/src/sandbox/"><code class="language-plaintext highlighter-rouge">v8/src/sandbox/</code></a> — sandbox, tablas, tags.</li> <li><a href="https://chromium.googlesource.com/v8/v8/+/refs/heads/main/src/common/ptr-compr-inl.h"><code class="language-plaintext highlighter-rouge">v8/src/common/ptr-compr-inl.h</code></a> — compresión/descompresión.</li> <li><a href="https://chromium.googlesource.com/v8/v8/+/refs/heads/main/src/heap/"><code class="language-plaintext highlighter-rouge">v8/src/heap/</code></a> — spaces, GC.</li> <li><a href="https://chromium.googlesource.com/v8/v8/+/refs/heads/main/src/objects/"><code class="language-plaintext highlighter-rouge">v8/src/objects/</code></a> — definiciones de <code class="language-plaintext highlighter-rouge">JSObject</code>, <code class="language-plaintext highlighter-rouge">Map</code>, <code class="language-plaintext highlighter-rouge">Code</code>, etc.</li> <li><a href="https://chromium.googlesource.com/v8/v8/+/refs/heads/main/src/wasm/"><code class="language-plaintext highlighter-rouge">v8/src/wasm/</code></a> — wasm runtime y trusted instance data.</li> </ul> <p>Bibliografía pública orientada a explotación:</p> <ul> <li><a href="https://saelo.github.io/"><code class="language-plaintext highlighter-rouge">saelo</code>’s exploits and posts</a> — referencia obligatoria.</li> <li><a href="https://googleprojectzero.blogspot.com/">Project Zero blog</a> — sobre todo los writeups de <em>In-the-Wild</em> de Chrome.</li> <li><a href="https://github.com/"><code class="language-plaintext highlighter-rouge">@_manfp</code>, <code class="language-plaintext highlighter-rouge">@kuqd</code>, <code class="language-plaintext highlighter-rouge">@__qaz__</code> Pwn2Own writeups</a> — caza de bugs modernos.</li> <li><a href="https://groups.google.com/g/v8-dev">V8 dev list</a> — discusiones de diseño del sandbox.</li> </ul> <p>En el próximo post montamos <code class="language-plaintext highlighter-rouge">addrof</code> y <code class="language-plaintext highlighter-rouge">fakeobj</code> modernos sobre un build de <code class="language-plaintext highlighter-rouge">d8</code> con pointer compression. Hasta entonces, te recomiendo levantar el lab con las flags de la sección 3 y jugar con <code class="language-plaintext highlighter-rouge">%DebugPrint</code> hasta tener intuición de qué hay en cada slot de un <code class="language-plaintext highlighter-rouge">JSObject</code>.</p>]]></content><author><name></name></author><category term="browser-internals"/><category term="exploit-dev"/><category term="v8"/><category term="javascript"/><category term="exploitation"/><category term="sandbox"/><category term="internals"/><summary type="html"><![CDATA[Montaje de un laboratorio con d8, flags y logs útiles, modelo de memoria de V8 con pointer compression, qué vive en la heap cage frente al trusted space, y por qué existe el V8 sandbox.]]></summary></entry></feed>